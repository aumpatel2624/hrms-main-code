# Open questions

> **TEMPLATE — not filled in yet.** Run the `grill-me` skill. Rows arrive here as the project is
> interrogated; an empty file means nobody has asked yet, not that nothing is unknown.

Known unknowns for **this project**. **Adding a row here is always better than guessing.** A guess
that reaches the code becomes an assumption nobody remembers making.

Anyone adds to this file. `grill-me` fills it during interrogation; `system-design` adds whatever
the design surfaced; `update-docs` closes rows once answered — moving the answer into `PRD.md`,
`DOMAIN.md` or `RULES.md` and citing it here.

**This file is project-specific and starts empty on a fresh clone.** Questions about the *starter* —
what it does not do and what closing a gap costs — are permanent and live in
[docs/conventions/60-limits.md](../conventions/60-limits.md). Start there: its *Decide before you
build* section is the five answers that are expensive to get late, and anything the client cannot
answer yet becomes a row below.

| ID | Question | Blocks | Asked | Answer |
|---|---|---|---|---|
| Q-26 | `Appraisal` (module 16, ADR-032) has zero cross-module effect on completion in source — no link to Payroll/`SalaryStructureAssignment` anywhere. Real-world performance-management systems often tie a completed appraisal's final score to a salary revision or bonus recommendation. Does Apidel want that connection, and if so what should trigger it (an HR-reviewed manual step referencing the Appraisal, or something automatic)? | Nothing blocks today — a real business/product decision, not an engineering gap | 2026-09-11 | |
| Q-25 | `Gratuity` (module 15, ADR-031) has zero statutory tax-exemption logic in source — real-world gratuity payouts are frequently exempt from income tax up to a jurisdiction-specific cap (e.g. India's ₹20 lakh limit), but this is a Regional (India-specific) rule, not something core Payroll models. Does Apidel need this, and if so is it in scope for the Regional module (17)? | A Regional-module follow-up, if ever asked for | 2026-09-11 | **Checked against the real Regional module source (module 18, ADR-034) and confirmed still open** — its entire gratuity contribution is one seeded `Gratuity Rule` record with zero tax-exemption logic of any kind (no "exempt"/"lakh"/"10(10)"/"statutory" anywhere in the source spec directory). This was never something Regional's source was going to answer; stays open as a genuine business question. |
| Q-24 | `Arrear` (module 12, ADR-028) reproduces source's positive-only delta exactly — a retroactive salary *decrease* (backdated demotion, overpayment correction) produces no deduction arrear at all, matching source's `if diff > 0` behavior. Does Apidel want retroactive clawback support (a negative arrear reducing a future payslip), or is positive-only correct for how they actually run payroll corrections? | Nothing blocks today — a real business decision about how retroactive pay corrections should work, not an engineering gap | 2026-09-11 | |
| Q-23 | `EmployeeOtherIncome` (module 12, ADR-028) is built as real self-service data collection (an employee can declare rental/interest/freelance income today), but has zero current consumer — its only real use in source is feeding the income-tax annualization engine, which `OPEN-QUESTIONS.md` Q-21 already defers pending "Tax & Exemptions." Employees can start using the screen now; nothing reads the data yet. | A Tax & Exemptions follow-up touch once that module exists | 2026-09-11 | **Closed — module 14 (Tax & Exemptions) is `EmployeeOtherIncome`'s first real consumer, summed directly into the annualization.** → `DECISIONS.md` ADR-030 |
| Q-22 | `SalarySlip`/`PayrollPeriod`/`PayrollSettings` (module 11 foundation, ADR-027) carry zero Employee-role self-service access, matching module 10's own "HR-configuration data, not self-service" precedent — but a payslip is exactly the kind of document a real client's employees usually expect to see their own copy of (unlike a Salary Structure or a Payroll Period, which really are HR-internal). Should an Employee be able to read their own submitted Salary Slips? | Whether Payroll — Run gets a real employee-facing "my payslips" view, on this branch or a follow-up | 2026-09-11 | |
| Q-21 | `SalarySlip`'s income-tax annualization/breakup (module 11, ADR-027) hard-depends on `Income Tax Slab` for its marginal-rate table — omitted entirely for now, same forward-dependency shape as Q-19. | A Payroll — Run follow-up touch once Tax & Exemptions is built | 2026-09-11 | **Closed — built in module 14** as `computeIncomeTaxBreakup` in `utils/incomeTaxCalc.js`, ported faithfully (annualization, exemption clawback, progressive slab tax, period apportionment), snapshot fields added to `SalarySlip`. → `DECISIONS.md` ADR-030 |
| Q-20 | `Payroll Correction` (module 11, ADR-027) is deferred in its entirety — its whole output in source is `Additional Salary` (Payroll — Adjustments & Incentives) + `Employee Benefit Ledger` (Payroll — Benefits) records, neither built yet. Nothing to write it into today. | Follow-up once both destination modules exist | 2026-09-11 | **Closed — built in module 13.** Both blockers ADR-028 named are gone (`Additional Salary` shipped module 12, `Employee Benefit Ledger` shipped module 13); the "circular dependency with `Arrear`" ADR-028 worried about turned out to have never been circular — source's `Arrear` reads `Payroll Correction`, not the reverse. → `DECISIONS.md` ADR-029 |
| Q-19b | Salary Slip Loan, Salary Slip Timesheet, and Payroll Entry's Overtime Slip orchestration (module 11, ADR-027) are **permanently out of scope**, not a forward-dependency deferral — they depend on a "Lending" app, a `Timesheet` doctype, and an `Overtime Slip` doctype respectively, none of which exist anywhere on this project's 17-module board. Only comes back if Apidel explicitly asks for loan- or timesheet-based pay — a business decision, not an engineering one. | Nothing today; would need an explicit client ask to ever reopen | 2026-09-11 | |
| Q-19 | `SalaryStructureAssignment.incomeTaxSlabId` (module 10, ADR-026) should be a real ref to `Income Tax Slab`, mandatory whenever the structure has a tax-flagged deduction component — but "Tax & Exemptions" doesn't exist yet. Omitted entirely for now, same forward-dependency shape as Q-15. | A Payroll — Structure & Assignment follow-up touch once Tax & Exemptions is built | 2026-09-11 | **Closed — built in module 14.** `incomeTaxSlabId`/`taxDeductedTillDate`/`taxableEarningsTillDate` added, validated at assignment save time. Old vs. new regime is just two different `Income Tax Slab` records, no regime field anywhere. → `DECISIONS.md` ADR-030 |
| Q-18 | `SalaryStructure.employeeBenefits`/`maxBenefits` and `SalaryStructureAssignment.payrollCostCenters` (module 10, ADR-026) belong to "Payroll — Benefits" and "Payroll — Adjustments & Incentives" respectively, neither built yet — both omitted entirely for now. | Follow-up touches once those two modules are built | 2026-09-11 | **Closed in full.** `payrollCostCenters` half: permanently out of scope (module 12/ADR-028 — a genuine GL dead end, not a deferral). `employeeBenefits`/`maxBenefits` half: retrofitted onto `SalaryStructure`/`SalaryStructureAssignment` in module 13 (ADR-029), the shared `Employee Benefit Detail` embedded shape. → `DECISIONS.md` ADR-028/ADR-029 |
| Q-17 | The payroll-formula evaluator built for module 10 (ADR-026, `utils/payrollFormula.js`) is deliberately scoped to a static full-cycle-no-leave preview at assignment time — does its variable/function set need to grow once Payroll — Run (module 11) needs the *real* attendance-derived payment-days context for actual Salary Slip formula evaluation, or is a second, richer evaluator warranted there instead of extending this one? | Payroll — Run's own design, when that module starts | 2026-09-11 | The same evaluator, extended context only (`paymentDays`/`totalWorkingDays`/running `grossPay`/`netPay` added; `evaluateComponentTable`/`evaluateFormula` unchanged) — no second engine. → `DECISIONS.md` ADR-027 |
| Q-16 | This module's `Employee Checkin` shift-occurrence resolution (ADR-025) is a deliberately simplified version of source's general algorithm (no exhaustive adjacent-day/366-day bounded search) — does it hold up against real check-in patterns once used, or does a real edge case (e.g. an overnight shift with an unusual buffer configuration) surface that needs the fuller algorithm? | Nothing blocks today — flagged as a fidelity gap to revisit if real usage surfaces a mismatch | 2026-09-11 | |
| Q-15 | `Shift Type.overtimeType`/`Shift Assignment.overtimeType` (module 9, ADR-025) should be real refs to `Overtime Type`, but Payroll (module 10+) doesn't exist yet — omitted entirely for now (not even a placeholder field), same forward-dependency shape as Q-13/Q-14. | A Shift & Attendance follow-up touch once Payroll is built | 2026-09-11 | |
| Q-14 | `LeaveEncashment.perDayEncashmentAmount` (module 8, ADR-024) should be derived from `Salary Structure Assignment.leave_encashment_amount_per_day`, but Payroll — Structure & Assignment (module 11) doesn't exist yet — kept as a manually-entered field for now, same forward-dependency shape as Travel's `expenseType` (Q-13) and module 3's `InterviewType.expectedSkillSet` before module 6 retrofitted it. | A Leave Encashment follow-up touch once Payroll — Structure & Assignment is built | 2026-09-10 | Retrofitted in module 10 (ADR-026): `LeaveEncashment.perDayEncashmentAmount` defaults from the employee's current `SalaryStructureAssignment.leaveEncashmentAmountPerDay` when not explicitly supplied. → `DECISIONS.md` ADR-026 |
| Q-27 | `Expense Claim Advance`'s entire mechanism (module 17, ADR-033) depends on `Employee Advance`, which the research confirmed does not exist anywhere in this codebase and is not a future module on this project's 17-module board either — source's own spec disclaims ownership of it. Dropped entirely, not deferred (there is no future module to retrofit into, unlike Q-13/Q-14/Q-15's pattern). | Nothing today — informational only. If Apidel ever wants real cash-advance tracking, `Employee Advance` would need to be scoped as a brand-new module first, outside this port's original 17. | 2026-09-12 | |
| Q-13 | `TravelRequestCosting.expenseType` (module 7, ADR-023) should be a real ref to `ExpenseClaimType`, but Expenses (module 16) doesn't exist yet — kept as free text for now, same forward-dependency shape as module 3's `InterviewType.expectedSkillSet` before module 6 retrofitted it. | A Travel follow-up touch once Expenses is built | 2026-09-10 | **Closed — built in module 17.** `TravelRequestCosting.expenseType` becomes a real `expenseTypeId` ref to `ExpenseClaimType`. → `DECISIONS.md` ADR-033 |
| Q-3 | Without a general ledger (ADR-016), how should Expense Claim/Payroll/Full and Final Statement (ADR-020) track "paid" once money actually moves — a manual "mark as paid" action with an amount/date/reference, or something that still integrates with whatever accounting system Apidel already uses outside this app? Full and Final Statement's `markAsPaid` (module 4) took the manual-action answer as a working default — revisit here if Apidel wants real accounting integration for any of the three. | Payroll and Expenses module design (Full and Final Statement already shipped with the manual-action default) | 2026-09-10 | **Closed for Expense Claim — module 17 uses the same manual-flag pattern as Full and Final Statement** (`isPaid` boolean, flipped by a `markExpenseClaimAsPaid` action, no amount/date/reference captured), a deliberate match to source's own immediate-payment-action semantics rather than routing through the `AdditionalSalary` payroll-ingestion primitive. → `DECISIONS.md` ADR-033 |
| Q-4 | Per-screen data scoping (ADR-016 DEV-2) needs a concrete mechanism: does `UserRoles` grow a matrix (role × screen → scope value) instead of one `dataScope` field, or does scope move onto the menu-permission rows (`60-limits.md`'s own suggestion)? Also needs an answer for how "approver-scoped" resolves `employee` from `frappe.session.user`'s equivalent (session `userId` → Employee lookup) efficiently on every list call. | Leaves module (first to need approver-scoping) | 2026-09-10 | `UserRoles.roles[]` grows an optional per-row `dataScope` override (inherits the role-level value when unset — zero migration); a new `APPROVER` scope value; `resolveRequestEmployee`/`getEmployeesApprovedBy` resolve session→Employee and the approver set. → `DECISIONS.md` ADR-024 |
| Q-5 | The three background jobs this project does keep (auto-attendance, leave accrual/expiry, leave encashment — ADR-016) need a real scheduler; this starter has none (`60-limits.md`). Node-cron in-process, OS cron hitting an endpoint, or something else — and does "single process assumed" (`60-limits.md`) make double-firing a real risk worth guarding against now? | Leaves and Shift & Attendance modules | 2026-09-10 | No new dependency (AGENTS.md forbids adding one without asking, and the user is offline) — a `SchedulerRunLog` collection + `setInterval` day-granularity runner in `server.js`, single-process by design (documented limitation, not solved). → `DECISIONS.md` ADR-024 |
| Q-6 | Real org data (`apidel-org-chart.csv`) has two pairs of near-duplicate department strings ("PR and Social media" / "PR and social media"; "Corporate Recruitment" / "Corporate Recruitment & Facility Management") — normalize to one canonical spelling each when Organization Setup is built, or are these actually two distinct departments? | Organization Setup module's Department seed | 2026-09-10 | Normalize. → `DOMAIN.md` HRMS spine entities (Department/Designation) |
| Q-7 | Should Recruitment's public job board (ADR-019) also let candidates apply anonymously (create a Job Applicant with no session)? This starter has **no HTTP rate limiting at all** (`60-limits.md`) — an anonymous write endpoint is spam/abuse surface this project hasn't decided how to guard yet. Module 3 built the public *listing* only; HR enters applicants manually for now. | Whether Recruitment gets a real self-service apply flow, and whether rate-limiting becomes its own piece of work first | 2026-09-10 | |
| Q-8 | `Job Opening`/`Job Offer` vacancy checks against `Staffing Plan` (ADR-019) are unbuilt — `Staffing Plan`/`Staffing Plan Detail` don't exist until module 5 (Employee Career Events). Once they do, should Recruitment's validation be retrofitted to enforce them, or is unlimited-vacancy hiring acceptable going forward? | Module 5, when it's built — and Recruitment needs a follow-up touch after | 2026-09-10 | Retrofitted in module 5 (ADR-021, no company-hierarchy version). → `DECISIONS.md` ADR-021 |
| Q-9 | `Job Applicant.source == "Employee Referral"` status-sync (write back onto `EmployeeReferral` when its linked Job Applicant's status changes) is still unbuilt. Its sibling half — Interview Feedback's per-skill `Skill Assessment` ratings — closed in module 6 (ADR-022): `InterviewFeedback.skillAssessment[]`, `InterviewType.expectedSkillSet[]`, and `Designation.skills[]` were all retrofitted into modules 1/3 once `Skill` existed. | A Recruitment follow-up touch for the reverse referral sync, whenever it's prioritized — nothing else blocks on it | 2026-09-10 | Skill Assessment half → `DECISIONS.md` ADR-022 |
| Q-10 | This project's dev database has zero `Country`/`State`/`City` rows and zero generic `User` (the starter's pre-HRMS login-account model, `User.countryId`/`stateId`/`cityId` all required) — found while verifying Recruitment's role matrix, which needed a throwaway `User` to test as HR User/Employee-role. Nothing HRMS-specific seeds these yet. Does Apidel need real geography master data seeded, or should `User`'s required country/state/city be reconsidered once `Employee.userId` self-service provisioning (deferred, ADR-018) actually gets built — a real login account might not need full postal geography for every HR-created Employee login. Reproduced again in module 5's own `verify` (still zero rows; still needed throwaway ones). | Whichever module first provisions a real self-service `Employee` login | 2026-09-10 | |
| Q-11 | `POST /cities` returns a 500 with a raw Mongoose validation message (`City validation failed: countryId: Path 'countryId' is required.`) leaked directly to the client, instead of the generic "Internal server error" `30-api.md` requires on a 500 — found live while creating a throwaway City for module 5's `verify`. Pre-existing in the starter's own City controller (`apps/server/controllers/v1/location.controller.js` or similar), unrelated to HRMS. Worth a small `fix-bug` pass, or leave until something else touches that controller? | Nothing blocks on this — informational leak, not a data-integrity issue | 2026-09-10 | |
| Q-12 | Logging in as a throwaway `User` whose email matches one used by an *earlier* module's `verify` session 500s with a MongoDB duplicate-key error on `loginattempts.userEmail` — that earlier session's `User` was (soft-)deleted during its own cleanup, but the `LoginAttempt` row survives (no cascade), and auth's login-attempt lookup/upsert isn't idempotent against a stale row for a reused email; one observed variant even produced a row with `userEmail` missing entirely, colliding on `null` for the *next* caller too. Found live in module 6's `verify` (`verify.hruser@example.com`/`verify.employee@example.com`, reused from module 3's session) — worked around by deleting the specific stale rows after confirming they were orphaned, not fixed at the source. Should every module's throwaway-account emails be made unique (e.g. timestamped, as later ad-hoc workarounds in this project's own history already do), should `LoginAttempt` cascade-delete with its `User`, or should the login path upsert instead of insert? | Nothing blocks on this today (each module can route around it once aware), but it will keep recurring for any module that reuses a throwaway email pattern | 2026-09-10 | Fixed at the source (issue #9): `recordFailedAttempt`/`recordSuccessfulLogin` in `apps/server/services/authService.js` now key their lookup/upsert on `userEmail` (the field with the real unique index) instead of `userId`, and re-point `userId` on the surviving row when an email is reused by a new account. No cascade delete needed. Commit `63a8bf4`, merged `4b9db8f`. |
| Q-2 | `client-docs` on the email trigger module (2026-09-09) found that the `EmailFor` edit form cannot correctly re-save a row whose `triggerKey` is an `unassigned.*` placeholder (from `backfillEmailForTriggerKeys` in `seed/index.js`). The Trigger dropdown (`triggerOptions` in `apps/admin/src/entities/index.js`) is built only from `EMAIL_TRIGGERS` (the code registry) filtered to unclaimed-or-own — an `unassigned.*` value was never in that registry, so it can never appear as a selectable option. The field renders empty on edit; saving either fails validation (nothing picked) or silently reassigns the row to a real, different trigger. Two real rows in the dev database are affected today: "Contact Enquiry" and "test". Should `triggerOptions` inject the record's current value as a synthetic, clearly-labelled ("not a real event — reassign or remove") option when it isn't in the registry, so these rows stay editable without accidentally being reassigned? | Editing (renaming, deactivating, or reassigning) any legacy/backfilled `EmailFor` row without an unintended trigger change | 2026-09-09 | |
| Q-1 | `verify` on the email trigger module (2026-09-08) found duplicate active `MenuMaster` rows for five URLs — `/email-for`, `/email-template`, `/email-setup`, `/client`, `/project`. **Correction from the original finding** (`client-docs`, 2026-09-09): for the email screens at least, the two copies sit under two *different* `MenuGroupMaster` documents — an older "CMS" group (2026-08-05) and a newer "Mail" group (2026-08-16) — not one group with a duplicated child row as first thought. Apparently from a non-idempotent reseed or migration. Server-side permission enforcement (`checkPermission`) is unaffected, but the admin client's own menu-id-to-permission resolution (`MenuContext.findMenuIdByUrl`, first-match-wins over the flat tree) can silently resolve the wrong row, so a role granted access via today's role editor may not show write buttons even though the server would allow the action. The real "test" role's saved matrix still only references the old 2026-08-05 ids. Should these rows (and their duplicate groups) be deduplicated, should the seeder be made to upsert by URL instead of by name+group, and should existing roles' matrices be re-pointed at the surviving ids? | Reliable non-admin permission checks/UI for the five affected screens; any future menu row on `/client` or `/project` inherits the same risk | 2026-09-08 | |

- **Blocks** — what cannot be built until this is answered. If nothing, say "nothing" and it is
  safe to proceed under a stated assumption.
- **Answer** — leave empty while open. When answered, record where the answer now lives, e.g.
  `→ RULES.md INV-4`.

## Assumptions in force

Where work proceeded without an answer, record the assumption and what breaks if it is wrong. These
are the first things to re-check when the client changes their mind.

| ID | Assumption | If wrong |
|---|---|---|
| | | |
