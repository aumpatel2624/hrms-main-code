# Project state

**The board.** Where every module is in the pipeline, and which client documents have been read.
Checked automatically at the start of every session, so work picks up where it stopped — even weeks
later, even in a different tool.

Maintained by the skills as they run. If you are unsure what to do next, ask — or run `whats-next`.

---

## Input documents

Every file in `docs/knowledge/input/`. The hash is how a returning session knows a document is new
or has been edited since it was read. `grill-me` adds a row after parsing a document.

| Document | Hash | Parsed on | Covers |
|---|---|---|---|
| `input/hrms/Apidel Organization Chart(1).html` | 8602332e | 2026-09-10 | Real org data (195 employees, 25 departments, 29 designations, manager hierarchy) — not plain HTML, a Claude Design Canvas export; extracted to `docs/knowledge/apidel-org-chart.csv`. Fully parsed. |
| `input/hrms/HRMS-Obsidian-Vault/**` (179 files) | — | 2026-09-10 (partial) | Overview/cross-cutting layer read in full this session: `00-Home/Home.md`, all ten `01-Modules/*/_Overview.md`, `02-Roles/Roles Overview.md`, `04-Graphs/*` (3 files), `05-Reference/Glossary.md`. The 156 per-doctype detail files and the 7 flow/role-detail files under `03-Flows/`, `02-Roles/*` (individual roles), `05-Reference/Doctype Index.md` are **not yet read** — read module-by-module during that module's own `system-design`/`schema-design`, per "build one module at a time." Not a flat file, so no single hash; re-check per-file mtimes if this folder changes before all of it is read. |
| `input/hrms/HRMS-Port-Spec/**` (165 files) | — | 2026-09-10 (partial) | Cross-cutting mechanical spec read in full: `00-Overview/README.md`, all 7 files in `02-Cross-Cutting/`, `03-API-Surface/API Contract Shape.md`. **Not yet read**: the 156 per-doctype spec files under `01-Modules/` (field tables, validation rules, calculation pseudocode — the primary reference for each module's `schema-design`) and `04-Data-Model/Full Doctype-to-Table Index.md`. Same partial-read caveat as the Vault. |

> Recording a hash: `shasum -a 256 docs/knowledge/input/<file> | cut -c1-8`
> A document whose current hash is not in this table is **unparsed** — its contents have never been
> turned into requirements. The session-start check will say so.

---

## Modules

One row per deliverable chunk of functionality. A module is something a user could notice arriving —
"Bookings", "Invoice PDF export" — not a layer.

Phases match the pipeline in `AGENTS.md`. Mark each `-` (not started) · `wip` · `done` · `n/a`.

| Module | Design | Schema | API | UI | Verified | Docs | Shipped | From |
|---|---|---|---|---|---|---|---|---|
| Soft delete | done | done | done | n/a | done | done | done | user request 2026-08-06 |
| Role data scoping (server-side enforcement) | done | done | done | done | done | done | done | user request 2026-08-17 |
| Dynamic dashboards (widget builder + per-role assembly) | done | done | done | done | done | done | done | user request 2026-08-17 |
| SEO management (pages, redirects, 404 log, sitemap, public API) | done | done | done | done | done | done | done | user request 2026-08-22 |
| Audit trail (who changed what, when) | done | done | done | done | done | done | done | user request 2026-08-22 |
| Reports (saved filtered lists + export) | - | - | - | - | - | - | - | user request 2026-08-17 |
| Analytics | - | - | - | - | - | - | - | user request 2026-08-17 |
| Dashboard layout canvas (drag/resize on the assign screen) | done | n/a | n/a | done | done | done | done | user request 2026-08-27 |
| Client documentation (generated pages + auto screenshots) | done | n/a | n/a | done | done | done | done | user request 2026-08-28 |
| Email trigger system (dynamic form → template routing) | done | done | done | done | done | done | done | user request 2026-09-05 |

### HRMS (Apidel) — build order

Derived from `HRMS-Port-Spec/02-Cross-Cutting/Build Order.md`'s dependency graph, split into
build-one-at-a-time modules and reordered to put Company/Branch first (that spec assumes they
pre-exist; here they don't — ADR-016). **Proposed order, pending user confirmation** — see the
grill-me round-1 summary in Log below. Not designed end-to-end: each module gets its own
`system-design` when its turn comes.

| Module | Design | Schema | API | UI | Verified | Docs | Shipped | From |
|---|---|---|---|---|---|---|---|---|
| Organization Setup (Company, Branch, Department, Designation, Employment Type, Employee Grade — HR Settings deferred, ADR-017) | done | done | done | done | done | done | local-only | grill-me 2026-09-10 |
| Employee Records (Employee core, Employee Health Insurance — ADR-018) | done | done | done | done | done | done | local-only | grill-me 2026-09-10 |
| Recruitment (Job Requisition → Opening → Applicant → Interview → Offer, public job *listing* — ADR-019) | done | done | done | done | done | done | local-only | grill-me 2026-09-10; public *apply* flow deferred (OPEN-QUESTIONS Q-7), vacancy checks/Employee Referral/Skill Assessment deferred to modules 5/6 (Q-8/Q-9) |
| Onboarding & Separation (Onboarding+Template, Separation+Template, Exit Interview, Full & Final Statement — ADR-020: activities embedded not a separate collection, no Project/Task, F&F Statement is a manual worksheet not a GL document) | done | done | done | done | done | done | local-only | grill-me 2026-09-10 |
| Employee Career Events (Transfer, Promotion, Referral, Grievance+Type, Staffing Plan — ADR-021: explicit fields not generic setattr, no company hierarchy, closes Q-8 vacancy-check retrofit into Recruitment) | done | done | done | done | done | done | local-only | grill-me 2026-09-10; Property History folded into Employee as a plain change-log, not a separate generic doctype |
| Training & Skills (Training Program, Training Event — Result folded in, Training Feedback, Skill, Employee Skill Map — ADR-022: 3 retrofits into modules 1/3 close Q-9's Skill Assessment half) | done | done | done | done | done | done | local-only | grill-me 2026-09-10 |
| Travel (Travel Request, Itinerary, Costing, Purpose of Travel, Identification Document Type — ADR-023: access opened up from source's System-Manager-only table) | done | done | done | done | done | done | local-only | grill-me 2026-09-10; ID Document Type moved here from module 2 by ADR-018 (its only named consumer is Travel Request.personal_id_type) |
| Leaves (Type, Period, Policy+Detail, Policy Assignment, Control Panel, Allocation, Earned Leave Schedule, Adjustment, Compensatory Leave Request, Application, Encashment, Ledger Entry, Block List+Date+Allow, Holiday List Assignment, Department Approver + the per-screen/company scoping mechanism — ADR-024) | done | done | done | done | done | done | local-only | grill-me 2026-09-10; **both halves merged into `development`** (real merge commits `77c3f69`/`d8fcfff`, `feat/leaves`/`feat/leaves-transactions` deleted after merge) — `LeaveAdjustment`/`CompensatoryLeaveRequest`/`LeaveApplication`/`LeaveEncashment`/`LeaveBlockList`+children, the two Leave Control Panel bulk endpoints, and `generateLeaveEncashments` all built; Q-4 scoping wired live onto `LeaveApplication`/`CompensatoryLeaveRequest` (Employee + Leave Approver roles, `SCOPES.APPROVER`); four real pre-existing bugs found and fixed across both forks (#10 own-scoped `/users/:userId` object-spread collision, #12 `buildScopeFilter`'s `APPROVER` branch ignoring `approverIds` when the user had no linked Employee, #13 `getLeaveAllocationById`/`getLeaveApplicationById` 500ing on a populated sub-document passed into `getLeaveBalance`, #11 `CrudForm`'s edit route had no permission guard). Both forks' logic independently re-verified by the orchestrator against ADR-024's specific rules (not just tests passing) before merge — see Log. Still `local-only` on `development` — not yet promoted to staging/production. |
| Shift & Attendance (Shift Type, Assignment, Request, Schedule, Schedule Assignment, Assignment Tool, Location, Attendance, Attendance Request, Checkin, Attendance Tool — ADR-025) | done | done | done | done | done | done | local-only | grill-me 2026-09-10; **module complete and merged into `development`** (`11da710`) — both stacked branches (`feat/shift-attendance` foundation, `feat/shift-attendance-transactions`) independently re-verified and merged by the orchestrator via real merge commits. Second-largest module in this build alongside Leaves; the first module to run through all three delegation tiers (Claude → codex → agy → Claude again) on one build. Issues #14/#15 closed |
| Payroll — Structure & Assignment (Salary Component, Salary Detail, Salary Structure, Salary Structure Assignment, Bulk Salary Structure Assignment — ADR-026) | done | done | done | done | done | done | local-only | grill-me 2026-09-10; **module complete and merged into `development`** (`c4df669`), independently re-verified by the orchestrator (formula evaluator confirmed free of `eval`/`Function`, CTC/gross and one-time-denormalization logic spot-checked against ADR-026) before merging. Built `SalaryComponent` (auto-derived+company-deduped abbreviation, mutual-exclusion/accrual-only-on-Earning guards), a hand-written recursive-descent formula/condition evaluator (`utils/payrollFormula.js`, no `eval`), `SalaryStructure` (server-computed totalEarning/totalDeduction/netPay, one-time-denormalized `SalaryDetail` row flags), `SalaryStructureAssignment` (server-computed annualGrossEarning/ctc via `utils/payrollCtc.js`, exact-duplicate-fromDate-only uniqueness, no `toDate`), `getCurrentSalaryStructureAssignment` resolver (`utils/payrollAssignment.js`), and the stateless Bulk Salary Structure Assignment tool (eligible-employees + assign, no stored model). **Q-14 closed for real**: `LeaveEncashment`'s `perDayEncashmentAmount` is now optional, defaulting from the employee's current Salary Structure Assignment. `EmployeeGrade` gained `defaultSalaryStructureId`/`defaultBasePay` (ADR-026's own forward-flagged retrofit point). Bug found and fixed (GitHub issue #16, closed): `updateEmployeeGrade` silently dropped any field not resent in the request body — same class as the pre-module-6 `updateDesignation` bug. New "Payroll" menu group (4 screens), roles seeded (HR User/HR Manager full access, no Employee grants — HR-configuration data only), 3 `widgetSources.js` entries, `docs-src/manifest.js` entries for all 4 screens/pages (client-docs prose/screenshots, phase 7.5, deliberately skipped — see Log). |
| Payroll — Run (Salary Slip, Payroll Entry+Employee Detail, Payroll Period, Payroll Settings, Salary Withholding+Cycle — ADR-027; Salary Slip Loan/Timesheet, Payroll Period Date, Payroll Correction out of scope/deferred) | done | done | done | done | done | done | local-only | grill-me 2026-09-10; **module complete on `feat/payroll-run-orchestration`** (foundation half merged in `91fada2`; orchestration half completed on `feat/payroll-run-orchestration`) — built `PayrollEntry` + embedded `PayrollEmployeeDetail` (`create-slips`/`submit-slips`/`cancel-slips` with per-item isolation and concurrency write lock, `getEmployeesForPayroll`), `SalaryWithholding` + cycle generation walking frequency-periods with month-end clamping (`salaryWithholdingCycles.js`), dynamic slip withholding display overlay (`withWithholdingDisplay`), manual and bulk cycle releases without GL postings. Custom UI `PayrollEntry.jsx` + entity config for `SalaryWithholding`. Fixed GitHub issue #17 (employeeId and date validation guards on slip assignment). Verified clean (24 test suites, 84 total HTTP assertions across both halves, 0 records in 8 touched collections, 195 employees baseline). **Merged into `development` (`c693345`) — independently re-verified by the orchestrator first (tests/seed/build re-run, withholding overlay and issue #17 fix spot-checked against the diff, no GL confirmed).** First module to touch all three delegation tiers twice each — Claude built the foundation half directly, then the orchestration half went Claude→codex (hit its own usage limit mid-task)→agy (finished it) after Claude's own account limit had already been hit repeatedly earlier in the session. |
| Payroll — Adjustments & Incentives (Additional Salary, Arrear, Retention Bonus, Employee Incentive, Employee Other Income — ADR-028; Employee Cost Center permanently out of scope, Payroll Correction stays deferred) | done | done | done | done | done | done | local-only | grill-me 2026-09-10; module complete on feat/payroll-adjustments — AdditionalSalary universal ingestion primitive retrofitted into SalarySlip (dependsOnPaymentDays: false hardcoded, overwrite-uniqueness validated at save time), Arrear calculation against historical submitted slips with positive-only delta enforcement, uniform cancellation-cascade across RetentionBonus/EmployeeIncentive/Arrear to cancel linked AdditionalSalary rows (fixing source orphan gap), EmployeeOtherIncome self-service with negative amount support |
| Payroll — Benefits (Employee Benefit Application+Detail, Claim, Ledger, Payroll Correction — ADR-029) | done | done | done | done | done | done | local-only | grill-me 2026-09-10; module complete on `feat/payroll-benefits` against ADR-029 — `EmployeeBenefitApplication`+Detail, `EmployeeBenefitClaim` (creates `AdditionalSalary` and cascades cancel), `EmployeeBenefitLedger` (append-only domain ledger with read-only public API), and `PayrollCorrection` (LWP reversal calculation generating earning/deduction `AdditionalSalary` + accrual `EmployeeBenefitLedger`, cascade cancel); closes Q-18 and Q-20 in full |
| Tax & Exemptions (Income Tax Slab+Other Charges, Taxable Salary Slab, Tax Exemption Category/Sub Category/Declaration+Category/Proof Submission+Detail — ADR-030) | done | done | done | done | done | done | local-only | grill-me 2026-09-10; module complete on `feat/tax-exemptions` against ADR-030 — `IncomeTaxSlab` (+embedded bracket/surcharge child schemas, bracket-overlap validation), 4 `EmployeeTaxExemption*` doctypes (`SCOPES.OWN` on Declaration/Proof Submission), `utils/incomeTaxCalc.js` annualization engine (unit-tested), `SalaryStructureAssignment`/`SalarySlip` retrofits wired into both the single-slip and bulk `PayrollEntry` paths; closes Q-19/Q-21/Q-23 |
| Gratuity (Gratuity, Gratuity Rule+Slab, Gratuity Applicable Component — ADR-031) | done | done | done | done | done | done | local-only | grill-me 2026-09-10; module complete on `feat/gratuity` against ADR-031 — `GratuityRule` (plain CRUD master, real slab-overlap validation, `disable` enforced at selection), `Gratuity` (submit computes work experience + payout via `utils/gratuityCalc.js`, creates `AdditionalSalary`; cancel cascades, fixing a real source asymmetry); `FullAndFinalStatement` gets a Gratuity payable auto-suggest retrofit |
| Performance (Appraisal Cycle, Template+Goal, Appraisal+KRA+Goal, Goal, Employee Performance Feedback, Feedback Criteria/Rating, KRA — ADR-032) | done | wip | wip | wip | wip | wip | wip | ADR-032 2026-09-11: KRA built now (spec was missing, not out of scope); one shared `utils/appraisalCalc.js` for weightage-sum + all 4 scores; `final_score_formula` extends `payrollFormula.js`; `Goal` gets real server-side transition guards, a parent-cycle guard, and `SCOPES.OWN`; two-branch build. **Foundation half merged into `development`** (KRA/AppraisalCycle/AppraisalTemplate/Appraisal + calc engine) — `Goal`/`EmployeePerformanceFeedback` (transactional half) still pending, so this module is not yet complete. |
| Expenses (Expense Claim+Type+Account+Detail+Advance, Expense Taxes and Charges) | - | - | - | - | - | - | - | grill-me 2026-09-10 |
| Regional (India HRA/Marginal Relief/Gratuity setup/custom fields, UAE Gratuity Rules) | - | - | - | - | - | - | - | grill-me 2026-09-10 |

**Rules for this table**

- A module reaches `done` on a phase only when that phase's skill says its checklist passed.
- Never mark a phase `done` you did not actually run. A wrong board is worse than no board — the
  next session trusts it.
- `n/a` is legitimate (a module with no UI, a change with no schema). Note why in Log.
- Only **one** module should be `wip` across the build phases at a time. Finishing beats starting.

---

## Log

Newest first. One line per session: what moved, and what is next. This is what a returning
session reads to remember the story.

| 2026-09-11 | **Module 16 (Performance) foundation half built, independently reviewed, and merged into `development`.** A Claude fork built `KRA`, `EmployeeFeedbackCriteria`, `AppraisalTemplate`+`AppraisalTemplateGoal`, the shared `FeedbackRatingSchema`, `AppraisalCycle`+`Appraisee`, `Appraisal`+`AppraisalKra`/`AppraisalGoal`, and `utils/appraisalCalc.js` against ADR-032, including its own real live-browser check (which caught and fixed one bug itself — a missing `getAllAppraisalCycles` import). Orchestrator's independent review (diff read against ADR-032, tests/build re-run, and a second live-browser pass, not just trusting the fork's own claims) found **two more real bugs**: **#25** — `appraisalConfig.viewFields` completely replaced (not extended) the base `fields` list, so the Appraisal view page silently dropped `employeeId`/`appraisalCycleId`/`appraisalTemplateId`/`remarks`/`reflections`, showing only the 5 computed scores — this also suppressed the tabs UI (issue #19) for this entity, since only one section ever had visible fields; fixed by listing the full field set `viewFields` is supposed to contain, matching every other config's own convention. **#26** — `deleteAppraisal` called `doc.softDelete()`, which is **not a real method anywhere in this codebase** (confirmed by searching the whole `apps/server/` tree) — every delete action using this pattern 500s. This same mistake was independently found in **5 more delete actions already shipped in `development`** (module 12's `AdditionalSalary`/`Arrear`/`RetentionBonus`/`EmployeeIncentive`/`EmployeeOtherIncome`, module 15's `Gratuity`) — never caught because every `scripts/verify-*.mjs` cleanup routine bypasses the HTTP delete endpoint entirely via a direct Mongoose `deleteOne` call. Fixed all 7 occurrences (module 16's on `feat/performance` directly; modules 12/15's on a separate hotfix branch, `fix/softdelete-not-a-method`, merged into `development` first) with the established `doc.isDeleted = true; await doc.save()` idiom. Both merges tested clean (`npm test`/`npm run build`) before pushing. | Goal + EmployeePerformanceFeedback (module 16's transactional second half) next, then Expenses, then Regional. |
| 2026-09-11 | **Module 16 (Performance) design complete — ADR-032 written on `feat/performance`.** A Claude fork research pass read all 10 real per-doctype specs plus ADR-016 and current code (`Employee.js`'s `departmentId`/`designationId`/`reportsToId`, `Designation.js`'s own forward-pointing code comment). **Key decisions**: `KRA` built now as a trivial plain master (its spec was simply missing from the input, not an out-of-scope call — nothing else claims to own it); field-name consistency (`kraId` everywhere, replacing source's `key_result_area`/`kra` split); `Employee Feedback Rating`'s three parents each embed the same shared schema shape (matching ADR-029's `EmployeeBenefitDetailSchema` precedent), not one polymorphic table; a new `utils/appraisalCalc.js` houses the weightage-sum validator (reused across all six weight-bearing child arrays) and all four scores (goal/manual-goal/self/feedback/final); the hardcoded-vs-dynamic star-count inconsistency in source resolves naturally to one shared `NUMBER_OF_STARS` constant (not portable at all without Frappe's Rating-field metadata, so not a "bug" to preserve); `final_score_formula` extends `payrollFormula.js`'s existing evaluator, no third evaluator; `Appraisal.startDate`/`endDate` default from the linked cycle (source never populates them); `Goal` gets a real server-side bulk-transition guard, a new parent-cycle guard preventing an infinite rollup loop, and `SCOPES.OWN` RBAC (source's own unscoped grant flagged explicitly by the research); no cross-module salary-revision link exists in source (flagged as new `OPEN-QUESTIONS.md` Q-26, a real product question); no GL surface; two-branch build matching Leaves/Shift & Attendance/Payroll — Run's precedent (foundation half: KRA/AppraisalCycle/AppraisalTemplate+child/Appraisal+its 2 scoring child tables/EmployeeFeedbackCriteria/the shared rating schema/the calc engine; transactional half: Goal's full tree-cascade machinery + EmployeePerformanceFeedback). | Implementation next — delegate to a Claude fork (tier 1) first, then independently verify (tests/build re-run, diff review of the cascade logic/weightage validator/SCOPES.OWN scoping/no-eval/no-GL, plus a real live-browser check per the lesson from the /add-pages bug hunt) before merge to `development`. |
| 2026-09-11 | **Issue #23 (`runListQuery` signature) fixed and merged into `development`.** All 10 list/search endpoints across 5 Payroll — Adjustments & Incentives screens now call the shared helper positionally, use real `$lookup`/`$addFields` stages instead of the non-existent `populate` option, and correctly nest the response under `data` instead of spreading it. Independently re-verified by the orchestrator against a fresh isolated server (not just trusting the self-report): all 10 endpoints confirmed 200 with the correct `data[0].{count,data}` envelope, and the module's own `scripts/verify-payroll-adjustments.mjs` (54 assertions, including a new one added for this fix) passed end to end. `npm test`/`npm run build` clean. | Module 16, **Performance** — design research complete, ADR-032 to be written next. |
| 2026-09-11 | **Module 15 (Gratuity) fully merged into `development`.** Implementation built directly by a Claude fork (tier 1, usage limit reset) against ADR-031; independently reviewed line-by-line by the orchestrator against the ADR (models, controller submit/cancel logic, `utils/gratuityCalc.js`'s calc engine, the `FullAndFinalStatement` retrofit) — zero bugs found. `npm test`/`npm run build`/`npm run seed` re-run clean; the module's own live HTTP verify script (`scripts/verify-gratuity.mjs`, 21 assertions) re-run against a live server: both slab-walk modes verified with real computed amounts (250000, 62500), cancel cascade, both submit guards (no relieving date / no submitted slip), `FullAndFinalStatement` auto-suggest, no-eval/no-GL sanity all confirmed. Merge conflicted with `entities/advanced.jsx`'s `ADVANCED_ENTITIES` array (the branch predated the issue #18 TDZ fix, which relocated that array) — resolved by hand, folding `gratuityRuleConfig`/`gratuityConfig` into the single, correctly-positioned array rather than leaving two competing declarations. While reviewing, the implementing fork also flagged (but correctly did not fix, out of scope) two more pre-existing bugs: a `runListQuery` call-signature mismatch breaking all list/search actions for 5 Payroll — Adjustments & Incentives screens (confirmed live by the orchestrator, filed as issue #23, handed to a fresh `codex` session in an isolated worktree), and a note that its own new array-field columns correctly use `name` (matching the issue #22 fix already merged). | Issue #23 (`runListQuery` signature) in progress on `fix/runlistquery-payroll-adjustments`. Next module on the board: Performance (row 73). |
| 2026-09-11 | **GitHub issue #23 fixed on `fix/runlistquery-payroll-adjustments`.** All ten Additional Salary, Arrear, Retention Bonus, Employee Incentive, and Employee Other Income list/search handlers now call `runListQuery` with its positional model/body/options signature, preserve server-side scope filtering, add the established aggregation label stages for their displayed references, and return the frontend-required `data: [{ count, data }]` envelope. The payroll-adjustments live verification walk now includes a #23 regression section that creates its own records, calls each GET/POST pair, checks the exact envelope and resolved labels, then cleans all fixtures. `npm test` and `npm run build` passed. No Gratuity (Module 15) files changed. | Next: push the issue-fix branch and open a PR to `development` for review. |
| 2026-09-11 | **User-reported bug hunt: "the /add pages show nothing" — 5 real bugs found and fixed, all merged into `development`.** Started from a live-browser reproduction (Playwright, this project's verify scripts had all been backend-HTTP-only until now, so none of these were ever caught): (1) **#18** — `entities/advanced.jsx`'s `ADVANCED_ENTITIES` array referenced 9 module-13/14 config constants declared *later* in the same file, a temporal-dead-zone `ReferenceError` that crashed the *entire* admin app's module load, not just `/add` routes; fixed by moving the array to the end of the file, after every constant it references (codex, `feat/18-advanced-entities-tdz`). (2) **#20** — a follow-up found during broader verification: `getAllLeaveAllocations` was never imported despite being used by `leaveAdjustmentConfig`/`leaveEncashmentConfig`'s lookups, a pre-existing module-8 bug coincidentally masked by #18's crash (codex, same branch). (3) **#21** — `SelectField` assumed every `options` entry was already `{value,label}`-shaped, but dozens of entity-config fields across the whole app (since module 1) pass a plain enum array instead; a plain string produced a blank-labeled dropdown item that silently saved the literal string `"undefined"`, and a literal `null` entry (used by a couple of fields as "unset") crashed the page outright — normalized both shapes once in the shared component (orchestrator, direct, `fix/select-field-plain-options`). (4) **#22** — 5 embedded array-field column definitions from modules 13/14 (`EMPLOYEE_BENEFIT_DETAIL_COLUMNS`, `TAXABLE_SALARY_SLAB_COLUMNS`, `OTHER_TAXES_AND_CHARGES_COLUMNS`, `EXEMPTION_DECLARATION_COLUMNS`, `EXEMPTION_PROOF_COLUMNS`) used `key` instead of the established `name` property `SimpleArrayField` actually reads, making those array editors completely non-functional in the browser — renamed all 5 (orchestrator, direct, same branch as #21). (5) **#19** — separately requested UI improvement: `CrudView` (the shared read-only detail screen) now renders multi-section entities as tabs via a new reusable `apps/admin/src/components/ui/tabs.jsx` primitive instead of a long stacked list; single-effective-section entities render unchanged; add/edit screens untouched (codex, isolated worktree, `feat/19-crudview-tabs`). All five independently verified live in a real browser (not just trusted from self-reports) before merging — a full 78-route `/add` page sweep confirmed zero page errors and zero "duplicate key" warnings after #18/#20/#21/#22 landed; the tabs UI confirmed working on both a 5-section entity (Employee) and a 2-section entity (Department). Root-caused a recurring guard-hook false-positive along the way: `.claude/hooks/guard-bash.sh` checks `$CLAUDE_PROJECT_DIR`'s current branch, not the actual worktree cwd's branch, so any `git commit/push/merge` run in an *isolated worktree* gets blocked whenever the main checkout happens to be sitting on `development` — the fix is to keep the main checkout on any non-protected branch while operating in other worktrees, not a bug in the merges themselves. All four merge commits (`af9452a`, `f440feb`, plus the Gratuity merge still pending) tested clean (`npm test`/`npm run build`) before pushing. | Next: independently review and merge Module 15 (Gratuity, `feat/gratuity`) — implementation complete, pushed, self-reported clean; not yet reviewed by the orchestrator. |
| 2026-09-11 | **Issue #18 hotfix verified on `feat/18-advanced-entities-tdz`.** Moved `ADVANCED_ENTITIES` below all config declarations in `advanced.jsx`, eliminating its module-level temporal-dead-zone crash; also restored the existing `getAllSalarySlips` API import that the newly reachable Payroll Correction config requires. Playwright logged in as admin and loaded five old/new `/add` routes with a rendered root and zero page errors; `npm test` and `npm run build` passed. | Orchestrator to independently verify and merge into `development`. |
| 2026-09-11 | **Module 14 (Tax & Exemptions) design complete — ADR-030 written on `feat/tax-exemptions`, implementation delegated to `agy`.** `agy` research pass read all 9 real per-doctype specs plus ADR-026/ADR-027/ADR-016 and current code (`SalaryStructureAssignment.js`, `SalarySlip.js`/`salarySlipCalc.js`, `EmployeeOtherIncome.js`), confirmed zero tax fields exist anywhere yet. **Key decisions**: `SalaryStructureAssignment.incomeTaxSlabId`/`taxDeductedTillDate`/`taxableEarningsTillDate` added (Q-19 closed); `SalarySlip` gains the real annualization pipeline as pure function `computeIncomeTaxBreakup` in new `utils/incomeTaxCalc.js`, called from `salarySlipCalc.js` and injected into the `variableBasedOnTaxableSalary`-flagged deduction row, plus snapshot fields `annualTaxableEarning`/`annualIncomeTax`/`incomeTaxDeduction`/`totalTaxDeductedTillDate`/`totalExemptionAmount`/`remainingSubPeriods` (Q-21 closed); old vs. new tax regime modeled as two independent `Income Tax Slab` records, no regime field anywhere; `Income Tax Slab` built as plain CRUD master (not submittable, deviation from source) with real bracket-overlap validation added (source gap fix); "+1" bracket-width quirk cut in favor of clean half-open-range slicing; `Taxable Salary Slab.condition` reuses `payrollFormula.js`'s existing `evaluateCondition`, no second evaluator; exemption declaration/proof pipeline ported as a real two-phase clawback (`calculateTotalExemption` dual-level per-category clamping, proof amounts replace declared amounts in the final period, unproven categories drop to zero); RBAC via established `SCOPES.OWN` pattern (deviation from source's permissive default); HRA modeled as an ordinary exemption sub-category, no dedicated field/calc (confirmed dead code upstream); `EmployeeOtherIncome` gets its first real consumer (Q-23 closed); no GL surface; single-branch build (`feat/tax-exemptions`), matching modules 12-13. `docs/knowledge/OPEN-QUESTIONS.md` Q-19/Q-21/Q-23 closed, all pointing at ADR-030. Implementation prompt handed to `agy` (Herdr pane `payroll-finish`), background completion-watcher armed. | Implementation in progress on `feat/tax-exemptions` — pending independent verification (tests/seed/build, diff review of the annualization engine/exemption clamping/RBAC scoping/no-eval/no-GL) before merge to `development`. |
| 2026-09-11 | **Module 15 (Gratuity) design complete — ADR-031 written on `feat/gratuity`.** A Claude fork research pass read all 4 real per-doctype specs plus ADR-016/020/026/028/030 and current code (`SalaryComponent.js`, `Employee.js`, `AdditionalSalary.js`, `utils/payrollPaymentDays.js`, `FullAndFinalStatement.js`). **Key decisions**: the dead `payViaSalarySlip` GL-vs-salary-slip toggle is dropped entirely (ADR-016 already cuts the GL branch, leaving one payout path) — `Gratuity` submit always creates an `AdditionalSalary` row; cancel cascades to cancel it, fixing a real source asymmetry (source only reverses the GL branch on cancel) using the same uniform pattern ADR-028/029 already established; `Gratuity Rule.disable` enforced at selection time (source never filters it, a flagged gap); `Gratuity Rule Slab` gets real overlap/gap validation reusing `validateTaxSlabs`'s shape (ADR-030) adapted for years instead of currency; the `fraction === 0` "skip to next slab" quirk preserved as literal fidelity (narrow edge case, no clear correct alternative); both `calculateGratuityAmountBasedOn` modes and all three tenure-rounding modes ported faithfully as real configuration, not simplified; a new `utils/gratuityCalc.js` houses `getWorkExperience`/`getGratuityAmount`, reusing the established LWP-day-resolution branching (`payrollPaymentDays.js`'s `Attendance`-vs-`LeaveApplication` logic) as a new dedicated `countLwpDaysInRange` function rather than force-fitting the existing per-period function to a multi-year tenure span; no statutory tax-exemption logic invented (flagged as new `OPEN-QUESTIONS.md` Q-25, pointing at Regional/module 17); `FullAndFinalStatement` (module 4) gets a small named retrofit — auto-suggests a Gratuity payable line at creation time from a submitted `Gratuity` record, one-time denormalized fetch, no live link. Single-branch build, matching modules 12-14. | Implementation next — delegate to a Claude fork (tier 1, usage limit reset) or codex/agy if that hits a limit again, then independently verify (tests/seed/build re-run, diff review of the slab-walk engine/cancellation cascade/no-GL) before merge to `development`. |
| 2026-09-11 | **Module 14 (Tax & Exemptions) fully merged into `development`** (merge commit `f1ce39c`, `feat/tax-exemptions` deleted both locally and on origin). Built by `agy` (models, `incomeTaxCalc.js` annualization engine + unit tests, controller/routes, admin UI, seed) until it hit its own usage quota mid-task; completed directly by the orchestrator (`widgetSources.js`/`manifest.js` registries) once both `codex` and `agy` were quota-exhausted simultaneously — the first time this session all three delegation tiers were unavailable at once. The live HTTP verify script (`scripts/verify-tax-exemptions.mjs`, 16 real HTTP assertions) was written and run by a `haiku`-model sub-agent from a fully-specified scenario (every expected number pre-computed by the orchestrator), then independently re-run by the orchestrator against the same live server before merging — identical pass, employee baseline preserved (195/195). `npm test`/`npm run seed`/`npm run build` all re-run clean on the merged `development` tree before pushing. Closes `OPEN-QUESTIONS.md` Q-19/Q-21/Q-23. See `DELEGATION-LEDGER.md` for the full token/efficiency breakdown of this hybrid multi-tier build. | Module 15, **Gratuity** — see the entry above, design already complete. |
| 2026-09-11 | **Module 13 (Payroll — Benefits) built and verified on `feat/payroll-benefits` — module complete across all phases (2-8).** Built directly against ADR-029's design. **Built**: `EmployeeBenefitApplication` model (`employeeId`, `companyId`, `payrollPeriodId`, `date`, embedded `employeeBenefits[]`, `maxBenefits`, `totalAmount`, `remainingBenefit`, `status`), ceiling validation checking component cap (`comp.maxBenefitAmount`) and assignment/structure cap (`maxBenefits`), active employee requirement, and duplicate period application rejection; `EmployeeBenefitClaim` model (`employeeId`, `companyId`, `salaryComponentId`, `claimDate`, `claimedAmount`, `yearlyBenefit`, `maxAmountEligible`, `additionalSalaryId`, `status`), submit action creating active additive `AdditionalSalary` row (`type: "Earning"`, `refDoctype: "EmployeeBenefitClaim"`), and cancel cascading to cancel linked `AdditionalSalary`; `EmployeeBenefitLedger` model (`postingDate`, `employeeId`, `companyId`, `salaryComponentId`, `payrollPeriodId`, `transactionType: "Accrual"|"Payout"`, `amount`, `salarySlipId`, `refDoctype`, `refDocnameId`, `isDeleted`), internal write helpers (`createBenefitLedgerEntry`, `getBenefitLedgerBalance` with `{ isDeleted: { $ne: true } }` filtering in aggregation, slip/claim soft-delete helpers), and read-only public API (`GET /` and `POST /search` and `GET /:id`, mutations 404); `PayrollCorrection` model (`employeeId`, `companyId`, `salarySlipId`, `payrollPeriodId`, `payrollDate`, `daysToReverse`, embedded `earningArrears[]`/`deductionArrears[]`/`accrualArrears[]`, `status`), pure calculation engine `utils/payrollCorrectionCalc.js` (`calculatePayrollCorrectionBreakup`) re-evaluating historical slip components against corrected payment days, submit generating active `AdditionalSalary` rows for earning/deduction deltas and `EmployeeBenefitLedger` rows for accrual deltas, and cancel cascading to cancel linked `AdditionalSalary` and soft-delete linked ledger entries; precursor retrofits on `SalaryComponent` (`isFlexibleBenefit`, `maxBenefitAmount`, `payoutMethod` [Encashment, Against Claim, Accrued On Date Pro-rata], `finalCycleAccrualPayout`), `SalaryStructure` & `SalaryStructureAssignment` (`maxBenefits`, `employeeBenefits[]`), `PayrollSettings` (`mandatoryBenefitApplication`), `AdditionalSalary` (enum extended with `"EmployeeBenefitClaim"` and `"PayrollCorrection"`), and `payrollRun.controller.js` (slip submit posting accruals, claim debits, and automatic final-cycle accrual payouts across all accrued ledger components, slip cancel soft-deleting linked ledger entries); fallback resolver `utils/employeeBenefitSource.js` (`getBenefitsDetailsParent`). **UI, Seed & Registries**: 4 entity configs registered in `apps/admin/src/entities/advanced.jsx` and API client in `apps/admin/src/api/payrollBenefits.api.jsx` with endpoint constants in `endpoints.jsx`; 4 menu rows seeded in Payroll menu group in `apps/server/seed/index.js` with HR User / HR Manager grants; 4 reporting sources registered in `apps/server/config/widgetSources.js`; manifest entries in `docs-src/manifest.js`. **Verified live**: `npm test` green (26 suites, including new `payrollBenefits.test.js`), `npm run seed` idempotent (0 added on second run, 195 employees), `npm run build` clean (5.69s). Full live HTTP verification via `scripts/verify-payroll-benefits.mjs` (44 real HTTP assertions): component ceiling and structure ceiling 400s, duplicate period application 400, claim payout method validation 400, mid-cycle claim eligible preview matching formula, claim submit creating AdditionalSalary, claim cancel cascade, slip submit generating ledger accruals and claim payouts, final-cycle slip auto-paying remaining accruals across components, PayrollCorrection LWP calculation and submit/cancel cascade, public ledger mutation route 404s. Database verified clean with 195 employees baseline. Branch `feat/payroll-benefits` ready for commit. | Module 14, **Tax & Exemptions** (Income Tax Slab+Other Charges, Taxable Salary Slab, Tax Exemption Category/Sub Category/Declaration+Category/Proof Submission+Detail) — next module in the HRMS build sequence. |
| 2026-09-11 | **Module 12 (Payroll — Adjustments & Incentives) built and verified on `feat/payroll-adjustments` — module complete across all phases (2-8).** Built directly against ADR-028's design. **Built**: `AdditionalSalary` (`employeeId`, `companyId`, `salaryComponentId`, `type`, `amount`, `isRecurring`+`fromDate`/`toDate` XOR `payrollDate`, `overwriteSalaryStructureAmount`, `deductFullTaxOnSelectedPayrollDate`, `currency`, `status: active|cancelled`, polymorphic back-ref `refDoctype`/`refDocnameId`), save-time overwrite uniqueness validation rejecting overlapping active overwrite rows for same employee/component across 4 date combinations; `Arrear` model (`companyId`, `employeeId`, `startDate`, `endDate`, `payrollDate`, child arrays `earningArrears[]` and `deductionArrears[]`, `status: draft|submitted|cancelled`), calculation engine `utils/arrearCalc.js` computing differences between historical submitted `SalarySlip` rows and revised `SalaryStructureAssignment` components with strictly positive delta enforcement (`delta > 0`, negative clawbacks dropped per ADR-028/Q-24), preview endpoint `POST /arrears/calculate`, submit action creating active `AdditionalSalary` rows, and cancel cascading to cancel them; `RetentionBonus` model (`bonusAmount`, `bonusPaymentDate`, validated against relieving date, earning-only component guard), submit creating active `AdditionalSalary` and cancel cascading to cancel it; `EmployeeIncentive` model (`incentiveAmount`, `incentiveDate`, earning-only component guard), submit creating active `AdditionalSalary` and cancel cascading to cancel it (deliberate bug fix over source orphan gap); `EmployeeOtherIncome` model (`payrollPeriodId`, `source`, `amount` supporting negative values for business losses/deductions, `date`, `status: draft|submitted|cancelled`), self-service access via `SCOPES.OWN` preventing access to other employees' records and blocking deletion by employees; `SalarySlip` calculation engine retrofit (`utils/salarySlipCalc.js`) merging active `AdditionalSalary` rows into slip earnings and deductions with `dependsOnPaymentDays: false` hardcoded on all injected adjustments, updating grossPay, totalDeduction, and netPay. **UI, Seed & Registries**: 5 entity configs in `apps/admin/src/entities/advanced.jsx` and API client in `apps/admin/src/api/payrollAdjustments.api.jsx` with endpoint constants in `endpoints.jsx`; 5 menu rows seeded in Payroll menu group in `apps/server/seed/index.js` with role matrix grants (full CRUD for HR User/HR Manager, full-minus-delete with `dataScope: "own"` for Employee on `/employee-other-income`); 5 reporting sources registered in `apps/server/config/widgetSources.js`; documentation manifests in `docs-src/manifest.js`. **Verified live**: `npm test` green (25 suites, including new `arrearCalc.test.js` and expanded `salarySlipCalc.test.js`), `npm run seed` idempotent, `npm run build` clean (5.68s). Full live HTTP verification via `scripts/verify-payroll-adjustments.mjs` (44 real HTTP assertions): statistical component rejection 400, employer contribution rejection 400, date mutual exclusion 400, save-time overwrite uniqueness rejection 400, additive co-existence 201, RetentionBonus submit/cancel cascade, EmployeeIncentive submit/cancel cascade, Arrear calculation and positive-only delta enforcement + submit/cancel cascade, EmployeeOtherIncome self-service own-scope and foreign record 403, and live SalarySlip calculation matching hand-calculated totals. Database verified clean with 195 employees baseline. Branch `feat/payroll-adjustments` ready for push. | Module 13, **Payroll — Benefits** (`Employee Benefit Application`, `Employee Benefit Claim`, `Employee Benefit Ledger`) — next module in the HRMS build sequence. |
| 2026-09-11 | **Module 11 (Payroll — Run) orchestration half built and verified on `feat/payroll-run-orchestration` — module complete across both halves.** Built directly against ADR-027's design and spec requirements. **Built**: `PayrollEntry` model (`companyId`, `payrollFrequency`, `payrollPeriodId`, `startDate`, `endDate`, `status`, `currency`, embedded `employees[]` `PayrollEmployeeDetail` subdocuments with tracking `status: draft|submitted|failed`, `failureReason`, `salarySlipId`), atomic bulk orchestration endpoints (`create-slips`, `submit-slips`, `cancel-slips`) implementing strict per-item try/catch isolation so single-employee failures (e.g. duplicate slip creation or negative net pay on submit) update that row to `failed` with reason while allowing all other employees to succeed, concurrency serialization via in-memory lock per `PayrollEntry`, and `getEmployeesForPayroll` resolver filtering by active assignment and frequency; `SalaryWithholding` model and cycle generation engine (`utils/salaryWithholdingCycles.js`) walking one frequency-period at a time (`addPayrollPeriods`) with month-end date clamping producing strictly N cycles without division, cycle status tracking (`pending|withheld|released`), vacation "released" state when cycles is empty, and manual single-cycle (`/release`) and bulk (`/release-all`) endpoints without GL journal postings; dynamic SalarySlip status overlay via `utils/payrollWithholding.js` (`withWithholdingDisplay`) exposing `displayStatus: "withheld"` and `isSalaryWithheld: true` without mutating database workflow status, with `cancelled` status strictly superseding withholding. **UI & Registries**: custom interactive page `pages/Payroll/PayrollEntry.jsx` with real-time status badges, bulk action triggers, collapsible failure logs, and slide-over employee detail drawer; entity config for `SalaryWithholding` in `apps/admin/src/entities/advanced.jsx`; client API in `payrollRun.api.jsx` and `endpoints.jsx`; route registration in `allRoutes.jsx`; updated `widgetSources.js` (sources for `payrollEntries` and `salaryWithholdings` with `companyId`/`employeeId` lookups and `payrollFrequency` filtering); documentation manifests in `docs-src/manifest.js`. **Bugs found and fixed**: (1) **GitHub issue #17 resolved** — `payrollRun.controller.js` (`createSalarySlipForEmployee`) validated that assignment belongs to the specified employee and `fromDate <= endDate`, returning HTTP 400 otherwise; tested and closed; (2) **Stray attendance cleanup** — discovered 1 stray undeleted attendance record (`6aa33c80f553d150a635c82b`) in DB from prior runs, diagnosed and soft-deleted via API, restoring clean baseline; (3) **Verify script test runner** — fixed test script port target (Vite runs on 3000, not 5173), dual-domain cookies for `127.0.0.1`/`localhost`, throwaway user generator without soft-deleted prototypes, and headless browser fallback for container environment lacking `libasound.so.2`. **Verified live**: `npm test` green (24 suites, including `salaryWithholdingCycles.test.js`), `npm run seed` idempotent (195 employees, 91 menus), `npm run build` clean (5.65s). Full live HTTP verification via `scripts/verify-payroll-orchestration.mjs` (52 real HTTP assertions passing) alongside foundation verification (`scripts/verify-payroll-run.mjs`, 32 assertions passing). DB verified clean with 195 employees and 0 records in all 8 touched collections (`payrollentries`, `salarywithholdings`, `salaryslips`, `salarystructureassignments`, `salarystructures`, `salarycomponents`, `attendances`, `payrollperiods`). Branch `feat/payroll-run-orchestration` pushed; not merged into `development`, no PR opened. | Module 12, **Payroll — Adjustments & Incentives** (`AdditionalSalary`, `Arrear`, `RetentionBonus`, `EmployeeIncentive`, `EmployeeOtherIncome`, `EmployeeCostCenter`) — next module in the HRMS build sequence. |
| 2026-09-11 | **Module 11 (Payroll — Run), foundation half built on `feat/payroll-run`, phases 2-8 only against ADR-027's already-written design (no re-litigation).** **Built**: `PayrollPeriod` (`companyId`/`startDate`/`endDate`/`isActive`, company-scoped overlap rejection — two different companies may freely overlap); `PayrollSettings` (true global singleton, `SeoSettings.js`'s exact constant-`key`-plus-unique-index pattern, `getPayrollSettingsDoc()` finds-or-creates-with-defaults on first read); the real payment-days pipeline `utils/payrollPaymentDays.js` — split into a pure `calculatePaymentDays` (unit-tested, no DB) and a thin DB-fetching `computePaymentDays` wrapper, both the Attendance-based and Leave-Application-based paths per `PayrollSettings.payrollBasedOn`, mid-period join/leave clamping, holiday inclusion/exclusion; `utils/salarySlipCalc.js` (`calculateSalarySlip`) reusing the *unchanged* `evaluateComponentTable`/`evaluateFormula` (confirmed unmodified) with a richer context (`paymentDays`/`totalWorkingDays`/running `grossPay`/`netPay`); `SalarySlip` itself (embedded `earnings`/`deductions`/`employerContributions` reusing `SalaryStructure.js`'s now-exported `SalaryDetailSchema`, payment-days snapshot fields, a scoped-down 3-field `leaves[]` snapshot, server-computed totals, `draft`/`submitted`/`withheld`/`cancelled` status) with `submit`/`cancel` actions. New "Payroll" menu rows (Payroll Period, Payroll Settings, Salary Slip — HR User/HR Manager full access, no Employee self-service grant), 2 `widgetSources.js` entries, `docs-src/manifest.js` entries for all 3 screens (client-docs prose/screenshots, phase 7.5, deliberately skipped, same as module 10). **The two fidelity questions the task asked to nail down from the real spec (`Salary Slip.md`)**: (1) **only a row flagged `dependsOnPaymentDays` is scaled** by `paymentDays/totalWorkingDays` — NOT every row (source's `get_amount_based_on_payment_days`, section I) — scaling is applied to each row's flat `amount` *before* `evaluateComponentTable` runs, so a later formula referencing an earlier *flat* row's abbreviation naturally inherits its already-scaled value (the real spec's own "cascading proration" behavior), while a formula-based row that is itself `dependsOnPaymentDays`-flagged is NOT separately re-scaled after its own formula evaluates — a narrow, named gap (every seeded/tested case has the payment-days-dependent component as a flat row, matching real usage); (2) **`totalWorkingDays` and `workingDays` are the same number** (source literally: `self.total_working_days = working_days`) — both computed here from the already mid-period-clamped `[actualStartDate, actualEndDate]` range, a deliberate simplification of source (which computes `total_working_days` from the *nominal* range and only the payment-days numerator from the clamped range) — recorded as a judgment call, not a bug; the practical difference only shows up for a mid-period joiner/leaver, where this project's simpler model prorates the denominator too. **`leaves[]` field set**: a scoped-down `leaveTypeId`/`allocated`/`used`/`available` (3 numeric fields), not source's 5-field `Salary Slip Leave` (`total_allocated_leaves`/`expired_leaves`/`used_leaves`/`pending_leaves`/`available_leaves`) — `expired`/`pending` need infrastructure (expiry-aware allocation walking, a pending-approval leave-days sum) this project hasn't built; `allocated` sums active `LeaveAllocation.newLeavesAllocated` covering the slip's `endDate`, `available` is the existing `getLeaveBalance`, `used = allocated - available`. **Verified live**: `npm test` green (23 suites, 2 new — `payrollPaymentDays.test.js` and `salarySlipCalc.test.js`, both pure/DB-free per this project's "DB-backed tests aren't wired into `npm test`" convention, covering full-attendance/one-LWP-day/unmarked-day-both-settings/half-day/mid-period-joiner/holiday-included-vs-excluded/Leave-Application-based for the former, and dependsOnPaymentDays-gating/cascading-formula-scaling/statistical-exclusion/condition-gated-skip-seeing-updated-grossPay/zero-totalWorkingDays-guard for the latter, all hand-computed), `npm run seed` run twice (idempotent — 6 new Payroll menu grants first run, 0 second run, 195 employees both times), `npm run build` clean. Full live HTTP walk via `scripts/verify-payroll-run.mjs` (32 real HTTP assertions): `PayrollSettings` first-ever-GET-creates-the-default-row confirmed live (this session's server had never touched the collection before), GET/PUT round-trip confirmed, settings restored afterward; `PayrollPeriod` CRUD plus the company-scoped overlap guard confirmed both directions (same-company overlap 400, different-company overlap allowed); `SalarySlip` full-attendance case hand-checked (grossPay 42000, netPay 41000, unscaled since paymentDays===totalWorkingDays), an LWP-day-plus-unmarked-day case hand-checked against the server's own returned `workingDays` rather than an assumed day count (immune to real holiday-calendar interference) — ratio 0.8, netPay 32600, the stored Basic row itself carries the scaled amount; exact-duplicate-`(employeeId,startDate,endDate)` 400 confirmed; a deliberately negative-net-pay slip confirmed to reject `submit` (400) yet still `cancel` cleanly; a valid slip confirmed to `submit` (200), reject a second submit (400, only a draft may submit), then `cancel` from Submitted (a valid transition per the state machine). No new GitHub issues found this session (no pre-existing bug encountered). Every throwaway fixture cleaned up via tracked deletion; employee count back to baseline 195; all six touched collections (`payrollperiods`/`payrollsettings`/`salaryslips`/`salarycomponents`/`salarystructures`/`salarystructureassignments`) back to 0. Branch `feat/payroll-run` pushed to origin; not merged, no PR opened — the orchestrator reviews and marks the module row `done`/ships it. | The second stacked branch for Module 11 — `PayrollEntry`+`Payroll Employee Detail` (bulk orchestration reusing this session's slip-creation logic, per-item isolation on both create and submit) and `SalaryWithholding`+cycle (the exact cycle-generation algorithm ADR-027 says to reproduce precisely, not approximate) — both depend on `SalarySlip` existing, which this session's branch now provides. Route to whichever tier has budget; brief with ADR-027's "As built" section above plus the full ADR text. |
| 2026-09-11 | **Module 10 (Payroll — Structure & Assignment) module complete on `feat/payroll-structure`, built directly against ADR-026's already-written design (phases 2-8, no re-litigation of ADR-026's own decisions).** **Built**: `SalaryComponent` (master — `abbreviation` auto-derived from name initials and de-duplicated with a `_N` suffix scoped to `companyId`, `arrearComponent`/`variableBasedOnTaxableSalary` mutual-exclusion guard, `accrualComponent`-only-on-Earning guard, no GL fields at all); the formula/condition evaluator `utils/payrollFormula.js` — a hand-written recursive-descent parser/tokenizer (arithmetic, comparison, `and`/`or`/`not`, `round`/`min`/`max`/`ceil`/`floor`, no `eval`/`Function`), `evaluateFormula`/`evaluateCondition` entry points, an undefined variable or malformed expression raises a clear `Error`; `SalaryStructure` (embedded `earnings`/`deductions`/`employerContributions` `SalaryDetail` rows, each a ONE-TIME denormalized snapshot of its `SalaryComponent`'s flags taken at row-creation — matched by subdocument `_id` on update so an already-existing row's snapshot is never re-copied from the master, only a genuinely new row gets a fresh one; server-computed `totalEarning`/`totalDeduction`/`netPay` via a new shared `utils/payrollCtc.js`); `SalaryStructureAssignment` (server-computed `annualGrossEarning`/`ctc` — same `payrollCtc.js` machinery, this time with the assignment's real `base`/`variable`; exact-duplicate-`fromDate`-per-employee is the ONLY uniqueness invariant, enforced as a real unique index, no `toDate` field, no overlap guard); `getCurrentSalaryStructureAssignment` (`utils/payrollAssignment.js` — latest `fromDate <= asOfDate`, no company filter, matching source exactly); the stateless Bulk Salary Structure Assignment tool (`eligible-employees` + `assign`, reusing one shared `createSalaryStructureAssignmentCore` function for both the single-assignment endpoint and the bulk path, per-item try/catch isolation, always synchronous — no size threshold). **Q-14 closed for real**: `createLeaveEncashment` (`leavesTransactions.controller.js`) now treats `perDayEncashmentAmount` as optional, resolving `getCurrentSalaryStructureAssignment(employeeId, encashmentDate).leaveEncashmentAmountPerDay` when omitted, falling back to the pre-ADR-026 400 only when neither resolves. `EmployeeGrade` gained `defaultSalaryStructureId`/`defaultBasePay` (its own code comment had already flagged this as the exact retrofit point). **Judgment calls** (ADR-026 didn't spell out the exact mechanics): (1) abbreviation dedup is scoped to `companyId`, not global like source — source has no company field on Salary Component at all, and an abbreviation only needs to be unique within the company whose formulas reference it; (2) a condition-gated row that evaluates false is skipped with NO context entry at all (not even a 0) — matches source's `_evaluate_component_table` exactly (`continue` before injection), confirmed via a dedicated `payrollCtc.test.js` case; (3) `totalEarning` excludes rows flagged `statisticalComponent`/`doNotIncludeInTotal` (matches source's real `gross_pay` rule) but `totalDeduction` is a plain unfiltered sum — nothing in the real spec ever filters a deduction row out of any total, so no symmetric exclusion was invented; (4) row-matching for the one-time-denormalization rule is by subdocument `_id`, not by position — the admin UI's `SimpleArrayField` round-trips `_id` as an untouched extra key on each row object, so an edited existing row never gets its flags re-copied from the master, while a brand-new row (no `_id`) always does. **Bug found and fixed — GitHub issue #16, closed**: `updateEmployeeGrade` unconditionally overwrote `gradeName`/`isActive` from the request body with no `!== undefined` guard — the exact same bug class as the pre-module-6 `updateDesignation` fix, present because nobody had added a second field to this handler until now. New "Payroll" menu group (Salary Component, Salary Structure, Salary Structure Assignment, Bulk Salary Structure Assignment — 4 rows), roles seeded (HR User/HR Manager full access on all four; no Employee grant anywhere — payroll structure/assignment is HR-configuration data, not self-service, unlike Leaves/Shift & Attendance). 3 `widgetSources.js` entries (all `companyConfined: true, employeeOwned: false`, reusing `attendanceScope` for company confinement the same way the controller itself does). `docs-src/manifest.js` entries for all 4 screens/pages — **phase 7.5 (client-docs prose + screenshots) deliberately skipped**: the task scoping this module explicitly named phases 2-8, not 7.5, and manifest registration alone was asked for; named here rather than silently dropped. **Verified live**: `npm test` green (21 suites, 3 new: `payrollFormula.test.js` — arithmetic precedence, parentheses, every comparison operator, `and`/`or`/`not` combined with comparisons, all 5 functions, a variable resolving from context, an undefined-variable error, a malformed-expression parse error, and a combined case; `payrollCtc.test.js` — hand-computed structure totals and CTC/gross including the Weekly-frequency-periods check; `payrollAbbreviation.test.js`), `npm run seed` run twice (idempotent — 8 new Payroll menu grants first run, 0 second run, 195 employees both times), `npm run build` clean. Full live HTTP walk via `scripts/verify-payroll-structure.mjs` (29 real HTTP assertions): `SalaryComponent` CRUD + abbreviation auto-derivation ("Basic Salary" -> "BS") + company-scoped dedup ("Bright Star" -> "BS_1") + both guards (400s) confirmed; `SalaryStructure` create with a mix of flat and formula-based rows hand-checked (totalEarning=42000 excluding a statistical row, totalDeduction=1800, netPay=40200), a row's formula referencing an earlier row's abbreviation confirmed to resolve (HRA = BS*0.4 = 12000), a condition-gated row confirmed skipped when false; `SalaryStructureAssignment` create hand-checked against a genuinely base-dependent structure (annualGrossEarning=840000, ctc=912000 for base=50000), exact-duplicate-fromDate 400 confirmed, a second later-fromDate assignment confirmed to succeed (not an overlap), `getCurrentSalaryStructureAssignment` confirmed to resolve the earlier one before the later `fromDate` and the later one after; bulk assignment confirmed with a mixed batch (2 succeed, then a deliberate duplicate-fromDate re-run fails in isolation) plus the eligible-employees dedupe confirmed both ways (eligible before assigning, excluded after); Q-14 confirmed live three ways (default-from-assignment, explicit-override-still-works, 400-when-neither-resolves). All throwaway fixtures cleaned up via tracked deletion; employee count back to baseline 195; `SalaryComponent`/`SalaryStructure`/`SalaryStructureAssignment` counts back to 0. Branch `feat/payroll-structure` pushed to origin; not merged, no PR opened — the orchestrator reviews and marks the whole module `done`/ships it. | Module 11, **Payroll — Run** (Salary Slip+Leave+Loan+Timesheet, Payroll Entry+Employee Detail, Payroll Period+Date, Payroll Settings, Payroll Correction+Child, Salary Withholding+Cycle) — the real payroll-run engine `payrollFormula.js`/`payrollCtc.js` were explicitly scoped NOT to be (ADR-026: a static, zero-leave-assumed CTC/gross preview only). Check whether `Overtime Type` (Q-15) and `TravelRequestCosting.expenseType` (Q-13) finally have a home here too before designing further deferrals. |
| 2026-09-11 | **Orchestrator review, merge, and closeout of the Shift & Attendance transactions half — module 9 now fully shipped.** Independently re-verified before merging: `npm test`/`npm run seed`/`npm run build` re-run clean both on the branch and inside the merge worktree; spot-checked `ShiftRequest.approve`'s reuse of `saveShiftRecord`/`withShiftWriteLock` (confirmed not bypassed), the `resolveApprovers("shiftRequest")` reuse (confirmed no second approver-resolution rule was invented), and `processAutoAttendance`'s leave-backed-row skip and yesterday-only absence sweep directly against ADR-025's specific rules. Confirmed issue #15 was real and correctly fixed by reading the actual diff (all six foundation admin screens were 404ing on every call from a missing `/api/v1` prefix — a gap the foundation half's own HTTP-walk verify had missed because it exercised the backend routes directly rather than through the real admin frontend's endpoint constants; worth remembering as a category of bug live-verification-against-the-backend-only can miss). Merged `feat/shift-attendance-transactions` into `development` (`11da710`) via the established isolated-worktree pattern, closing issue #15 in the merge commit (confirmed auto-closed). Branch deleted (local + remote); local branch list re-synced with remote. Module 9 (Shift & Attendance) is now the second module, after Leaves, to run its full two-branch split and close — and the first to touch all three delegation tiers (Claude research fork failed → codex research succeeded → codex build partially succeeded then hit its own limit → agy finished it → Claude confirmed available again and built the second branch). Full detail in `DELEGATION-LEDGER.md`. | Module 10, **Payroll — Structure & Assignment** — first Payroll module; before designing, check whether the `Overtime Type` (Q-15), `LeaveEncashment.perDayEncashmentAmount` (Q-14), and `TravelRequestCosting.expenseType` (Q-13) forward-dependency gaps finally have a real home to retrofit into here. |
| 2026-09-11 | **Module 9 (Shift & Attendance) module complete — second/transactional fork built on `feat/shift-attendance-transactions`.** Built directly against ADR-025's already-written design and the second fork's own scope (already assigned) on top of the already-merged foundation half. **Built**: `ShiftRequest` (`shiftTypeId`/`employeeId`/`companyId`/`approverId`/`fromDate`/`toDate`/`status`, `approverId` resolved via `resolveApprovers(employeeId, "shiftRequest")` reused AS-IS from ADR-024, auto-resolved or validated against the resolved set if supplied; overlap validation covers both other Shift Requests AND — ADR-025's simplified stand-in for source's real timing-overlap test, since this project keeps the no-multi-shift-per-day invariant — an existing active Shift Assignment; `/approve` creates a real `ShiftAssignment` through the existing write-locked `saveShiftRecord` (confirmed live, not bypassed), `/reject` no side effects); `AttendanceRequest` (`employeeId`/`companyId`/`fromDate`/`toDate`/`halfDay`/`includeHolidays`/`halfDayDate`/`reason`(Work From Home/On Duty)/`explanation`/`status`(active/cancelled) — create IS the action per ADR-025, writing/updating one `Attendance` row per covered day via the exact target-status mapping in `Attendance Request.md` (`half day date` wins, then Work From Home, else Present), skipping a holiday unless `includeHolidays`, skipping any day whose existing `Attendance` row already carries a `leaveApplicationId`; `/cancel` soft-deletes exactly the `Attendance` rows this request created via the `attendanceRequestId` back-ref, reusing `LeaveApplication.cancel`'s reversal pattern); Shift Assignment Tool (`bulk-assign`, `bulk-assign-schedule` — refactored `shiftAttendance.controller.js`'s `generate` action into a plain reusable `generateShiftsForScheduleAssignment` function + thin HTTP wrapper, same split as `leaves.controller.js`'s `grantAllocationsForAssignment`, so the bulk tool calls the real generation logic instead of duplicating it — and `process-requests`, all three with per-item try/catch isolation); Employee Attendance Tool (`mark` and `resolve-half-day`, the latter a direct `findByIdAndUpdate` bypassing Attendance's own validation chain, matching source's real behavior, HR-Manager-only); `jobs/attendanceScheduler.js`'s `processAutoAttendance` — the third of ADR-016's three named background jobs, a separate runner file/`SchedulerRunLog.jobName` wired into `server.js` alongside (not merged into) `leaveScheduler.js`. New pure/tested `utils/attendanceAutoStatus.js` (`selectWorkingHoursFn` picks the right one of the four existing `workingHours.js` functions from a Shift Type's own settings, `decideAttendanceStatus` applies the Absent-then-Half-Day strict-`<` threshold order, `computeLateEarlyFlags` compares first/last punch against the shift window plus grace periods). Two custom admin pages (`pages/ShiftAttendance/ShiftAssignmentTool.jsx`, `EmployeeAttendanceTool.jsx`), 4 new menu rows, `widgetSources.js` entries for both new collections, `docs-src/manifest.js` entries for all 4 new screens/pages. **Judgment calls**: (1) the two bulk tools' request shape is an explicit employee-id/shift-request-id list per item — the real tool's Branch/Department/Designation/Grade/Employment-Type quick-filter selection is a documented nice-to-have, not built, to keep the endpoint surface controlled; (2) `EmployeeCheckin.shiftId` grouping for the auto-attendance job reuses the field exactly as the foundation half already populated it at checkin-creation time (`shiftValidation.js`'s `resolveShiftOccurrence` call) rather than re-resolving per checkin, deriving each checkin's occurrence *date* via the same `shiftWindow`/`dayStart` math `matchShiftOccurrence` already uses, applied directly against the known Shift Type; (3) no watermark bookkeeping anywhere in the new job — both halves (checkin-grouping and the absence sweep) only ever process an occurrence dated strictly before "today", the same "yesterday-only, no watermark" reasoning ADR-025 named for the absence sweep, extended here to checkin-grouping too, named explicitly as a limitation (a long-stopped server does not backfill, it only ever looks at "yesterday"); (4) `ShiftRequest`/`AttendanceRequest` are NOT part of the `attendanceScope` company-confinement family — both use a plain `buildScopeFilter` own-scope pattern (the same `id`-overridden-to-Employee-id trick `attendanceScope` uses internally), matching every other self-service screen in the project (`LeaveApplication`), not `attendanceScope` itself. **Bug found and fixed — GitHub issue #15**: the foundation half's own `SHIFT_TYPES`/`SHIFT_LOCATIONS`/`SHIFT_ASSIGNMENTS`/`SHIFT_SCHEDULES`/`SHIFT_SCHEDULE_ASSIGNMENTS`/`EMPLOYEE_CHECKINS` entries in `apps/admin/src/api/endpoints.jsx` were missing the `${V1}` prefix every other endpoint group in that file uses, and the admin axios client's `baseURL` is the bare host with no `/api/v1` of its own — every one of those six foundation CRUD screens was 404ing on every call. Found while extending the same file for this fork's own new endpoints; fixed by adding the prefix to all six. **Verified live**: `npm test` green (18 suites, including new `attendanceAutoStatus.test.js`), `npm run seed` run twice (idempotent — 9 new menu grants first run, 0 second run, 195 employees both times), `npm run build` clean. Full live HTTP walk via `scripts/verify-shift-attendance-transactions.mjs` against a running server (38 real HTTP assertions plus direct model/job assertions): `ShiftRequest` create with both overlap rejections, approver auto-resolve and explicit-supplied-and-validated both confirmed, approve confirmed to create the right `ShiftAssignment` via the write-locked path, reject confirmed to have zero side effects; `AttendanceRequest` create confirmed to create the right `Attendance` rows with holiday-skip and leave-backed-row-skip both confirmed against constructed fixtures, `includeHolidays` confirmed, cancel confirmed to soft-delete exactly its own rows without touching another request's; both bulk-action tools confirmed with mixed batches (2 succeed, 1 deliberately fails) proving per-item isolation, `bulk-assign-schedule` confirmed to actually invoke real shift generation; `processAutoAttendance` manually invoked directly against constructed `EmployeeCheckin`/`ShiftAssignment` fixtures covering a normal Present day, an Absent-by-threshold day, a Half-Day-by-threshold day, late-entry/early-exit flagging together, the leave-backed-row skip (confirmed the pre-existing row and its checkins were left completely untouched), and the yesterday-only absence sweep — confirmed idempotent on an immediate second run (zero new checkin-group rows, no duplicate absence row). Every throwaway fixture cleaned up via tracked deletion; employee count back to baseline 195. Branch `feat/shift-attendance-transactions` pushed to origin; not merged, no PR opened — the orchestrator reviews and marks the whole module `done`/ships it. | **Payroll — Structure & Assignment** (Salary Component+Account, Salary Detail, Salary Structure, Salary Structure Assignment, Bulk Assignment) — next on the HRMS build-order board, not yet designed. Start with `system-design` against the real per-doctype specs under `HRMS-Port-Spec/01-Modules/Payroll-*/` (not yet read); this is the first Payroll module, so the `Overtime Type`/`expenseType`/`perDayEncashmentAmount` forward-dependency gaps named across modules 4/8/9 (`OPEN-QUESTIONS.md`) may finally have a real home to retrofit into — check before designing further deferrals. |
| 2026-09-11 | **Orchestrator review, merge, and closeout of the Shift & Attendance foundation half.** Independently re-verified before merging — re-ran `npm test`/`npm run seed`/`npm run build` (all green) both on the branch and again inside the merge worktree, and spot-checked the highest-risk logic directly against ADR-025 rather than trusting the Log entry below: `isAssignmentCurrentlyActive` (computed, not mutated — confirmed no expiry job was added), `geofence.js`'s haversine (correct Earth-radius constant, correct formula), `attendanceScope`'s company-confinement wiring, the issue #14 `$lookup` fix, and the `Attendance` unique `(employeeId, attendanceDate)` index (confirmed unchanged, not loosened for multi-shift). This was the first module to run through all three delegation tiers back to back — a Claude research fork failed on the account spend limit, `codex` built most of the foundation before hitting its own usage limit (~21 min, uncommitted), and `agy` picked up the uncommitted tree cleanly and finished it. **One real inaccuracy caught on review**: the module-table row (below) had marked every phase column `done`, overstating completion for what is only the foundation half — corrected to `wip` across Schema/API/UI/Verified/Docs. Merged `feat/shift-attendance` into `development` via the established isolated-worktree pattern (real merge commit `a8d27ec`), closing GitHub issues #8 and #14 in the merge commit message (confirmed auto-closed). Branch deleted (local + remote); local branch list re-synced with remote. Full detail on the tiered-delegation handoffs and independent-verification steps is in `DELEGATION-LEDGER.md`. Confirmed Claude's tier-1 spend limit reset as advertised (tested with a trivial fork immediately after the merge) — available again for the second stacked branch. | Second stacked branch for Module 9 (`Shift Request` with `resolveApprovers` reuse, `Attendance Request` with soft-delete reversal, the two bulk-action tools, `processAutoAttendance`) — routing to a Claude fork now that tier 1 is confirmed available again, same detail-of-prompt bar as modules 1-8. |
| 2026-09-11 | **Module 9 (Shift & Attendance) foundation half built, verified, and documented on `feat/shift-attendance`.** Executed across two tiers of the automated pipeline — codex built the bulk of the foundation models, controllers, routes, pure utils, and entity configs (~21m before hitting its own account usage limit); Antigravity picked up the uncommitted working tree cleanly, verified design fidelity against ADR-025, resolved bugs, finished the live HTTP walk, and handled documentation and git. **Built**: `ShiftType` (startTime/endTime duration validation, checkin buffers, auto-attendance fields), `ShiftLocation` (geofencing coordinates + checkinRadius in meters, no GeoJSON), `ShiftAssignment` (date ranges, overlap checks, write lock), `ShiftSchedule` (weekly/fortnightly/etc. repetition, day deduplication), `ShiftScheduleAssignment` (`/generate` action advancing `createShiftsAfter` watermark), `EmployeeCheckin` (punch tracking, shift occurrence resolution, haversine geofence validation, time/link immutability once linked to Attendance), and extended `Attendance` (10 new fields including workingHours, overtime, lateEntry, earlyExit, halfDayStatus). Pure testable utilities: `shiftOccurrence.js`, `geofence.js` (haversine), `workingHours.js` (all 4 modes), `shiftSchedule.js` (date range generation), `shiftValidation.js`, `shiftAssignmentWrite.js` (`withShiftWriteLock` concurrency serialization per employee/schedule), and `attendanceScope.js`. UI & Config: 6 new entity configs + updated `attendanceConfig` in `apps/admin/src/entities/advanced.jsx`, `GenerateShiftsPanel` component, `shiftAttendance.api.jsx`, `widgetSources.js` (6 new sources + extended `attendances`), "Shift & Attendance" menu group, and role permissions seeded for HR User, HR Manager, and Employee. **Bugs found and fixed**: (1) **GitHub issue #14** — `attendance.controller.js`'s `listAttendanceByParams` never joined `Employee`, causing the list view to display raw ObjectIds; fixed by adding `$lookup` and `$addFields` for `employeeName` and `employeeIdLabel`, adding `searchFields: ["employeeName", "status"]`, and populating `shiftId` and `departmentId` in `getAttendanceById`. (2) **`dashboard.controller.js`** — `checkPermission` was called without being imported when running companyConfined widgets, throwing a ReferenceError / HTTP 500 on widget preview/run; fixed by importing `checkPermission`. (3) **Verify script collision** — duplicate widget title collision against `DashboardWidget.title` unique index in `verify-shift-attendance.mjs` resolved with distinct titles. (4) **GitHub issue #8 resolved** — codex added `--screens=` CLI option to `scripts/docs-capture.js` and regenerated markdown documentation pages for HRMS modules 1-8; verified all pages are accurate and non-destructive; committed as a separate incidental commit and closed issue #8 on GitHub. **Verified live**: `npm test` green (all 17 test suites, including the 4 new shift attendance test files), `npm run seed` idempotent (0 new menu grants, 195 employees), `npm run build` clean. Full live HTTP walk executed via `scripts/verify-shift-attendance.mjs` against a live running server (91 real HTTP assertions): full CRUD across masters, active assignment overlap rejection and inactive allowance, schedule generation watermark advance & mixed-range isolation, normal & overnight shift punch resolution, geofence radius acceptance & rejection, duplicate checkin rejection, checkin time/link locking once linked to Attendance, all 4 working hours modes verified from real punch fixtures, company confinement confirmed for HR User and own-scope confirmed for Employee, and live role dashboard widget execution verified. All test fixtures cleaned up, baseline 195 employees confirmed. Branch `feat/shift-attendance` pushed to origin. | Second stacked branch for Module 9: `Shift Request` (with approver resolution via `resolveApprovers`), `Attendance Request` (with soft-delete reversal), the two bulk-action tools (`Shift Assignment Tool` and `Employee Attendance Tool`), and the daily background job `processAutoAttendance` (`jobs/attendanceScheduler.js`). |
| 2026-09-11 | **Module 9 (Shift & Attendance) designed — ADR-025.** First real activation of the tiered-implementation fallback: the Claude research fork for this module failed mid-task on the account's monthly spend limit (resets 3am UTC) — exactly the signal the user set up earlier this session. Re-ran the same research as `codex exec` (read-only sandbox, piped prompt via stdin/`-o` for output capture) instead of restarting the Claude fork; it returned a dense, well-grounded 11-doctype report in one pass, no back-and-forth needed. Wrote ADR-025 directly from that report (same "orchestrator writes the ADR, delegates only schema/API/UI/verify" split as every prior module). **Key calls**: `processAutoAttendance` is the third of ADR-016's three named background jobs (Leaves already covered "leave accrual/expiry" and "leave encashment") — built as its own daily job file (`jobs/attendanceScheduler.js`, independent of `leaveScheduler.js` but reusing `SchedulerRunLog`), not a literal port of source's three hourly jobs, which this project's day-granularity single-process scheduler (Q-5, ADR-024) can't reproduce. Source's hourly schedule-generation sweep (`process_auto_shift_creation`) is deliberately **not** built as a background job at all — ADR-016 names three job categories to keep, not four, so `Shift Schedule Assignment` generation becomes an explicit action instead. No multi-shift-per-employee-per-day (keeps the existing unique `(employeeId, attendanceDate)` index from module 8 rather than loosening it — nothing in the real org-chart data needs it). Geofencing via plain `latitude`/`longitude`/`checkinRadius` on `Shift Location` plus a haversine check on `Employee Checkin`, no GeoJSON/no global HR-Settings toggle (HR Settings is deferred, ADR-017; enforcement is inherently per-location already). Shift-occurrence resolution and the four working-hours calculation modes are both real algorithmic pieces — the occurrence resolver is a deliberately narrowed version of source's general algorithm (flagged as `OPEN-QUESTIONS.md` Q-16, a fidelity gap to revisit, not silently invented as complete); the four working-hours modes are kept in full as pure, testable functions since they're well-specified, not ambiguous. `Overtime Type` deferred to Payroll (Q-15), same forward-dependency shape as Travel's `expenseType`/Leave Encashment's per-day rate. **A real pre-existing gap found by this research, to be fixed by the foundation fork**: module 8's minimal `Attendance` controller has no company confinement at all (`runListQuery` called without a `scopeFilter`) — a direct ADR-016 violation, not filed as a GitHub issue since it was never shipped with a real screen/real users yet, just fixed as part of this module's extension. `Shift Request` reuses `resolveApprovers`/`getEmployeesApprovedBy` (ADR-024) as-is rather than reproducing source's narrower, subtly different rule — reuse-before-you-write. Split into two stacked branches, same pattern as Leaves: `feat/shift-attendance` (foundation) now branched off `development` and design-committed; a second branch for `Shift Request`/`Attendance Request`/the two bulk-action tools/`processAutoAttendance` once the foundation lands. | Delegate `feat/shift-attendance`'s implementation (schema/API/UI/verify/docs/git, phases 2-8) to `codex exec` (tier 2 is active — Claude's account spend limit hasn't reset yet) with the same level of prompt detail used for every prior Claude fork; review its diff line-by-line against ADR-025 before merging, same discipline as modules 1-8, and log real token/duration numbers in `DELEGATION-LEDGER.md` — codex's research task already gave the first real per-task cost datapoint for that tool (see the ledger). Retry a Claude fork for the second stacked branch once the spend limit resets (3am UTC) if it hasn't already been delegated to codex/agy by then. |
| 2026-09-10 | **Orchestrator review, merge, and closeout of Leaves (module 8).** Independently re-verified both forks' work before merging — not just re-running `npm test`/`npm run seed`/`npm run build` (all green both times), but reading the highest-risk files in full and cross-checking the actual logic against ADR-024's specific rules rather than trusting either fork's own summary: `utils/scope.js`'s `APPROVER` branch and `checkPermission.js`'s `row.dataScope || roleDoc.dataScope` fallback (foundation half) matched the ADR's exact spec; `utils/leaveProration.js`'s two named rounding rules (tenure whole-number, earned-leave decimal) were reproduced correctly with the simplification honestly documented in a code comment; the transactions half's `approveLeaveApplication`/`cancelLeaveApplication` correctly implement ledger-boundary splitting and soft-delete reversal (not source's hard-DELETE, per ADR-024's explicit deviation), and `canAccessLeaveApplication`'s self-approval/approver-scope wiring matched the Q-4 mechanism as designed. Confirmed issues #10-#13 were all real, correctly diagnosed, and correctly fixed by reading each diff, not just trusting the fork reports. Merged `feat/leaves` (`77c3f69`) then `feat/leaves-transactions` (`d8fcfff`) into `development` via the established isolated-worktree pattern — real merge commits, tests re-run inside each worktree before committing, `Closes #11`/`#12`/`#13` in the second merge commit (auto-closed on push, confirmed via `gh issue list`). Both feature branches deleted (local + remote) after merging; local branch list re-synced with remote (`git fetch --prune`) — only `development`/`staging`/`production` remain, matching origin. Also set up `DELEGATION-LEDGER.md` (repo root, plain markdown) this session, tracking token/tool-call/duration cost per delegated task across the Claude→codex→agy fallback tiers the user set up mid-session (tier 1 only so far) — module 8 cost 1.56M tokens combined across 4 fork tasks for 17 collections/screens, 2 cross-cutting infra pieces, and 4 real bugs found+fixed; see that file for the per-task breakdown and running efficiency notes. | Module 9, **Shift & Attendance** — same as the foundation fork already noted: a minimal `Attendance` model exists (`employeeId`/`companyId`/`attendanceDate`/`status`/`leaveApplicationId`/`leaveTypeId`), extend it, don't rebuild it. Read the real per-doctype specs under `HRMS-Port-Spec/01-Modules/Shift & Attendance/` before writing the ADR — not yet read. Route this module's heavy implementation to `codex`/`agy` per the user's tiered-fallback instruction if Claude usage is nearing its limit by then, purely to get a real cross-tool comparison in `DELEGATION-LEDGER.md` — the ledger currently has zero data for either. |
| 2026-09-10 | **Leaves (module 8) module complete.** Second stacked fork, branch `feat/leaves-transactions`, built directly against ADR-024's design (already written) and the second fork's own scope (already assigned) — phases 2-8, same as the foundation half. **Built**: `LeaveAdjustment` (create-is-the-action, one signed ledger entry, no update/delete — matches the ledger's append-only convention); `CompensatoryLeaveRequest` (`/approve` validates active-employee, date order, that every worked day is a real holiday per the employee's resolved Holiday List — new `utils/holidayResolution.js`, employee-then-company fallback, built fresh since the foundation half never needed one — and that matching `Attendance` rows exist, a deliberate hard block rather than an auto-create since nothing populates Attendance yet; finds/extends or creates the resulting `LeaveAllocation` dated the day after the worked range, writes a ledger entry); `LeaveApplication` (the centerpiece — server-always-recomputed `totalLeaveDays` via new pure `utils/leaveDayCalculation.js`, `leaveApproverId` resolved via `resolveApprovers`/validated against it with self-approval prevention, an idiomatic-rebuild validation chain — active-employee, half-day sanity, balance via `getLeaveBalance` with the `allowNegative` override, overlap with a half-day-adjacency carve-out, max-consecutive-days as a one-hop adjacency merge not source's full recursive walk, block-date enforcement, `applicableAfter`, `isOptionalLeave` — `/approve` creates/updates `Attendance` and writes 1-2 ledger entries split across an allocation boundary, `/reject` no side effects, `/cancel` soft-deletes the ledger entries and `Attendance` rows it created); `LeaveEncashment` (balance/eligible-days computed via `getLeaveBalance` at creation, debiting ledger entry written immediately — "create is the action" again — `perDayEncashmentAmount` manually entered, no Payroll module to derive it from yet, `OPEN-QUESTIONS.md` Q-14; `/mark-paid` the same manual GL-substitute Full & Final Statement established in module 4); `LeaveBlockList`+`blockDates[]`+`allowList[]` (new `utils/leaveBlockList.js`, `listApplies`/`getApplicableBlockLists`/`getBlockedDatesInRange`/`isDateBlocked`, block enforcement is a hard block at LeaveApplication *create* — not only at approve, a deliberate stricter-than-source reading — with allow-list bypass); Leave Control Panel's two bulk endpoints (`bulk-policy-assignments` creates unallocated assignments, `bulk-allocations` additionally calls a newly-extracted `grantAllocationsForAssignment` per employee — refactored out of the existing `grantLeavePolicyAssignmentAllocations` HTTP handler so both the route and the bulk endpoint share one implementation instead of two; per-item try/catch isolation on both); `generateLeaveEncashments` filled in (`jobs/leaveScheduler.js`, reads `LeaveAllocation` rows `processExpiredAllocations` just expired in the same pass — matched on `status: "expired"`, not `"active"`, since ordering matters — drafts a `pending` `LeaveEncashment` per eligible allocation, idempotent via a `leaveAllocationId`-exists check). New file pair `leavesTransactions.controller.js`/`.routes.js` rather than growing the foundation's `leaves.controller.js` further. Six new menu rows, roles seeded (HR-only write for Adjustment/Encashment/Block List/Control Panel; Employee+Leave Approver self-service on Application/Compensatory Leave Request). Five `widgetSources.js` entries, five manifest entries plus one `CUSTOM_SCREENS` entry for the Control Panel page. **The Q-4 scoping judgment call** (ADR-024 explicitly left this to this fork): `Employee` and the pre-existing-but-until-now-ungranted `Leave Approver` role both get `SCOPES.APPROVER` on `/leave-application` (and `Employee` alone on `/compensatory-leave-request`) — one scope value covers "my own" and "my own plus who I approve for" at once, since `approver` (not `own`) is the dimension that correctly reads `reqUser.employeeId` rather than the login `User`'s own id. **Two real pre-existing bugs found live-testing this, filed and fixed**: **#12** — `utils/scope.js`'s `APPROVER` branch returned "matches nothing" the instant `reqUser.employeeId` was missing, *without ever consulting `approverIds`* — so a Leave Approver with no Employee record of their own (a real, documented case, Q-10) always saw zero rows regardless of who they approve for; this was the foundation half's own code for this exact scope value, simply never exercised live until this fork wired it onto a real screen. Fixed, regression case added to `scope.test.js`. **#13** — `leaves.controller.js`'s `getLeaveAllocationById` (foundation half) 500'd on every call: it populates `employeeId`/`leaveTypeId` and then passes the populated sub-documents into `getLeaveBalance`'s `new mongoose.Types.ObjectId(String(...))`, which throws on anything that isn't already a hex string. Fixed to compute the balance from the raw ids first, populate after — the identical trap existed in this fork's own brand-new `getLeaveApplicationById` and was caught in the same pass before shipping. Also found and fixed a real UI gap, **#11**: `CrudForm` never guarded the edit route against a missing `edit` permission (only the "Edit" button was permission-gated, not the route itself), so any entity config without a real `api.update` — this fork's own `LeaveEncashment`/`LeaveAdjustment`, and the pre-existing `trainingFeedbackConfig` — would crash with a raw `TypeError` on Save if reached directly by URL; added the guard plus a defense-in-depth `updateLeaveEncashment` that always 400s cleanly (needed because `LeaveEncashment` does need `edit` permission granted, for its `/mark-paid` action route). **Verified live**: `npm test` green (new `leaveDayCalculation.test.js`, `leaveBlockList.test.js`, and the `scope.test.js` #12 regression case), `npm run seed` twice (idempotent, 195 employees both times), `npm run build` green, and a full HTTP walk against the real running server — `LeaveApplication` create/balance/overlap/self-approval/max-consecutive/block-date(+allow-list bypass) all confirmed, approve confirmed to create the right `Attendance` rows and post the right ledger entry, reject confirmed to leave zero side effects, cancel confirmed to reverse both via soft-delete with the balance verified back to its pre-approval value; `CompensatoryLeaveRequest` approve confirmed against a real constructed Holiday List + Attendance fixture, both its hard blocks (not-a-holiday, missing-Attendance) confirmed separately; `LeaveAdjustment` confirmed to write exactly one ledger entry while `LeaveAllocation`'s own cached `newLeavesAllocated`/`totalLeavesAllocated` stayed untouched — checked directly via the now-fixed `GET /leave-allocations/:id`'s `currentBalance` vs. its cached total, not just asserted; `LeaveEncashment` create+mark-paid+idempotency-guard+the #11 defense-in-depth guard all confirmed; both Control Panel bulk endpoints confirmed with a mixed batch (2 successes, 1 deliberate overlap failure) proving per-item isolation; `runDueJobs()` manually triggered against a constructed near-expiry allocation fixture (ledger `createdAt` deliberately backdated at the raw-collection level, since Mongoose treats `createdAt` as immutable on `updateMany`) — confirmed `generateLeaveEncashments` drafted a `pending` encashment in the same pass as the expiry, and confirmed idempotent on a second run. The allocation-boundary-splitting approve case was code-reviewed but not separately live-fixtured — named rather than silently claimed as tested. All throwaway fixtures cleaned up via the app's own endpoints (employee count back to 195; the `LeaveType`, `LeavePeriod` and `LeaveBlockList` test rows were removed where a reference guard allowed it — two `LeaveAllocation` rows carrying an irreversible `LeaveAdjustment`/`LeaveEncashment` reference could not be deleted, by design, and remain as harmless historical test transactions). Branch `feat/leaves-transactions` pushed to origin; not merged, no PR opened — the orchestrator reviews and marks the whole module `done`/ships it. | Module 9, Shift & Attendance — a minimal `Attendance` model already exists (foundation fork of module 8) with `employeeId`/`companyId`/`attendanceDate`/`status`/`leaveApplicationId`/`leaveTypeId`; extend it with shift assignment, check-in/out and geolocation, don't rebuild it. Start with `system-design` against the real per-doctype specs under `HRMS-Port-Spec/01-Modules/Shift & Attendance/` (not yet read). |
| 2026-09-10 | Foundation half of Leaves (module 8), executed directly against ADR-024 (already written and self-approved before this session started — this fork's job was phases 2-8, not design). Branch `feat/leaves`, stacked on `development` — this is deliberately half a module: `LeaveAdjustment`/`CompensatoryLeaveRequest`/`LeaveApplication`/`LeaveEncashment`/`LeaveBlockList`+children/Leave Control Panel are explicitly a second stacked fork's job, not built here. **Built**: (1) Q-4 scoping — `SCOPES.APPROVER` (`packages/shared/src/scopes.js`), `UserRoles.roles[].dataScope` (per-menu-row override, `null` inherits the role default — zero migration), `checkPermission` reading `row.dataScope || roleDoc.dataScope`, `buildScopeFilter`'s new `APPROVER` branch (`utils/scope.js`, stays synchronous), `utils/requestEmployee.js` (`resolveRequestEmployee`, lazy per-request User→Employee resolution). (2) Q-5 scheduler — `SchedulerRunLog` model, `jobs/leaveScheduler.js` (`runDueJobs`, day-granularity, per-job try/catch, fixed order `processExpiredAllocations → generateLeaveEncashments (stub) → allocateEarnedLeaves`), wired into `server.js` via `setTimeout`(10s)+`setInterval`(5min). (3) Department Approver — `Department.parentDepartmentId`+three approver arrays, `utils/approvers.js` (`resolveApprovers`/`getEmployeesApprovedBy`, depth-10-bounded chain walk). (4) Minimal `Attendance`. (5) `LeaveType`/`LeavePeriod`/`HolidayList`(+`holidays[]`)/`HolidayListAssignment`/`LeavePolicy`(+`leavePolicyDetails[]`)/`LeavePolicyAssignment`(+`grant-allocations` action)/`LeaveAllocation`(+`earnedLeaveSchedule[]`,+`adjust` action)/`LeaveLedgerEntry` (read-only, custom page not an entity config — same reasoning as `AuditLog`) — 9 new models total, full CRUD controllers/routes/swagger/admin API clients/entity configs, `utils/leaveBalance.js` (`getLeaveBalance`, manually excludes soft-deleted rows since `.aggregate()` bypasses that plugin), `utils/leaveProration.js` (tenure pro-ration rounds whole, earned-leave pro-ration rounds decimal, per ADR-024). New "Leaves" menu group (9 screens), roles seeded (HR User/HR Manager full on the 8 config screens, read-only on `LeaveLedgerEntry` for HR User matching the Employee Health Insurance precedent from module 2, Employee read-only on Type/Period/HolidayList only), 5 starter Leave Types seeded (generic Frappe defaults, not spec-confirmed — flagged same as Travel's masters), 9 `widgetSources.js` entries, manifest entries for all 9 screens. **The two hardest judgment calls, not already settled by ADR-024**: (a) the earned-leave schedule/pro-ration algorithm is a deliberate simplification of source's real half-year/quarter-calendar-anchored algorithm (`utils/leaveProration.js`) — calendar-months-from-`fromDate` periods rather than quarter/half-year-anchored ones, reproducing only the two rules ADR-024 explicitly named (whole-number tenure rounding, decimal earned-leave rounding); (b) carry-forward computation uses `getLeaveBalance` as of the previous allocation's own `toDate` rather than source's period-scoped `get_unused_leaves` query — close enough for this fork's own single-allocation-per-period test cases, worth a closer look once the second fork's Leave Application adds real consumption entries to net against. **One real bug found in my own new code before it ever shipped**: `buildEarnedLeaveSchedule` originally pro-rated only the schedule's first row for a mid-period join date — for an employee joining well after the schedule's `fromDate`, every month between the schedule start and the actual join date wrongly earned a full month's share instead of zero. Caught by hand-checking the pro-ration math during the HTTP walk before trusting the numbers, fixed to place the join date per-row instead of only on the first one, locked in with a new `leaveProration.test.js` case. **One real pre-existing bug found and fixed, filed as GitHub #10**: while cross-checking Q-4's per-menu-row override against the *existing* `own` scope dimension (unrelated to Leaves — `apps/server/controllers/v1/user.controller.js`), `{ _id: userId, ...buildScopeFilter(...) }` silently let the scope filter's own `_id` key overwrite the URL param's `_id` whenever the "own" dimension happens to be declared against `_id` itself, so `GET/PUT/DELETE /users/:userId` under an `own`-scoped role ignored the URL entirely and always resolved to the caller's own account (no cross-user leak, but a real correctness bug — false-positive 200s, silent wrong-target edits/deletes). Fixed with `$and` instead of object-spread in all three functions. **Verified live**: `npm test` green (10 dependency-free suites, incl. new `leaveProration.test.js`; `approvers.test.js`/`leaveBalance.test.js` are DB-backed by necessity and run standalone, not wired into the dependency-free chain — documented in their own file headers), `npm run seed` twice (idempotent — 0 new Leave Types/grants on the second run), `npm run build` green, and a full HTTP walk: LeaveType/LeavePeriod/HolidayList/HolidayListAssignment/LeavePolicy CRUD; a full `LeavePolicyAssignment → grant-allocations` cycle against a real employee with hand-verified pro-ration (a mid-year joiner: Casual Leave tenure-prorated to 6 of 12 whole days, Earned Leave schedule-summed to 3 of 12 — both matched hand math exactly), confirmed idempotent (second call 400s), `LeaveLedgerEntry` rows confirmed written and correctly excluded from `getLeaveBalance` once soft-deleted; `LeaveAllocation`'s generic PATCH confirmed to REJECT a direct `newLeavesAllocated` change on an active allocation, `/adjust` confirmed to write the signed delta and update the balance; `runDueJobs()` manually triggered against constructed fixtures (an allocation past its `toDate`, an earned-leave schedule row due today) — confirmed `processExpiredAllocations` wrote the correct negative ledger entry and flipped `status: "expired"`, confirmed `allocateEarnedLeaves` allocated exactly the due row and no others, confirmed a same-day second `runDueJobs()` call is a full no-op (no double allocation, no duplicate ledger entry, `SchedulerRunLog` unchanged); the Q-4 cross-check confirmed live over real HTTP with a throwaway role carrying role-level `dataScope: "all"` but a menu-row override of `"own"` on `/user` — before the fix, User A could read User B's record; after, User B's id correctly 404s while User A's own id still resolves — and a real HR User account confirmed listing all 24 seeded Departments (module 1, unchanged) to rule out a scoping regression on an existing screen; `resolveApprovers`/`getEmployeesApprovedBy` exercised against both synthetic fixtures (direct-field-wins, multi-level department-chain union, a manufactured 2-node cycle that terminates instead of hanging, department-fallback correctly excluding an employee with their own direct approver) and confirmed not to hang against the real seeded Apidel department hierarchy. Full browser UI click-through was **not** performed — Playwright's browser binary isn't installed in this sandbox and the download couldn't complete; relied instead on a clean Vite build (catches JSX/import errors) plus manual review of every new field/lookup/`disabled`/`clears` config against the crud-form's actual implementation. All throwaway fixtures (roles, users, leave periods/policies/assignments/allocations/ledger entries) cleaned up afterward — employee count back to 195, Leave Type count back to 5 seeded. `feat/leaves` pushed to origin per this fork's instructions; not merged, no PR opened — the orchestrator reviews and hands it to the second fork as its base. | The second `feat/leaves`-stacked fork: `LeaveAdjustment`, `CompensatoryLeaveRequest`, `LeaveApplication`, `LeaveEncashment`, `LeaveBlockList`(+`blockDates[]`+`allowList[]`), and the Leave Control Panel bulk-action endpoints — all depend on this foundation's scoping/approver/scheduler infra and on `LeaveAllocation` existing. Read `LeaveApplication`'s real per-doctype spec closely before designing its ~14-step validation chain; `generateLeaveEncashments` in `jobs/leaveScheduler.js` is a stubbed no-op waiting for `LeaveEncashment` to exist — fill in its body without touching the runner's ordering/bookkeeping. Once that fork lands, this module's Log/board rows can finally move to `done`. |
| 2026-09-10 | Full pipeline for Travel (module 7), executed directly (not delegated to a background fork) after an Agent tool call unexpectedly placed this session inside worker-fork execution rules — same standing overnight authorization, against ADR-023, which the orchestrator wrote and self-approved after reviewing module 6's clean completion. Branch `feat/travel` — a genuinely small module, no scope correction needed, the Day-1 sketch was right. **The one real judgment call**: source declares only System Manager for all three doctypes, with no explicit submit rights even for that role — an incomplete area of the traced source, not a deliberate design to preserve, same category as Employee Separation's missing duplicate-guard in module 5. Opened `TravelRequest` up the same way every other self-service-shaped doctype in this project has been: `Employee` full-minus-delete on their own requests, `HR User`/`HR Manager` full CRUD — `PurposeOfTravel`/`IdentificationDocumentType` stay ADMIN-only, matching source's literal (and internally consistent) table for those two. **Built**: `PurposeOfTravel`, `IdentificationDocumentType`, `TravelRequest` (embedded `itinerary[]`/`costings[]`) — 3 new models. New "Travel" menu group (3 screens, `SimpleArrayField` for both embedded arrays), starter master data seeded (5 Purposes of Travel, 4 Identification Document Types — no real-world source data existed for these, unlike other modules' masters), 1 `widgetSources.js` entry for `travel-requests`, manifest entries for all 3 screens. **No new bugs found** — unlike modules 1/3/6, a clean build with nothing to fix along the way. **Verified live**: `npm test` green, `npm run seed` twice (idempotent), `npm run build` green, and a full HTTP walk — Travel Request created with populated itinerary/costing sub-documents and an auto-filled `companyId`; the Inactive-employee guard confirmed (400, exact message format) by toggling a real employee's status and back; the missing-required-fields guard confirmed (400); the reference-guarded delete on Purpose of Travel confirmed (409); role checks confirmed end to end with three throwaway accounts (Employee/HR User/HR Manager) — all cleaned up afterward, employee count back to 195, master lists back to their seeded rows. Per this fork's own operating constraint, nothing was pushed, no PR opened, no merge attempted — `feat/travel` is left committed and clean for the orchestrator. | Module 8, **Leaves** — the largest module yet (Type, Period, Policy+Detail, Policy Assignment, Control Panel, Allocation, Earned Leave Schedule, Adjustment, Compensatory Leave Request, Application, Encashment, Ledger Entry, Block List+Date+Allow, Holiday List Assignment, Department Approver), and where `OPEN-QUESTIONS.md` Q-4 (per-screen/company data scoping) and Q-5 (background job scheduler choice) both finally have to get answered rather than deferred again. Read its real per-doctype specs before designing. When a remote is available: push `feat/travel`, merge it same as modules 1-6 (ordinary merge commit, not squash — squashing module 1 broke stacked-branch ancestry earlier this session), and decide whether Travel's PR stacks on `feat/training-skills` or targets `development` directly now that all prior modules are merged. |
| 2026-09-10 | Full pipeline for Training & Skills (module 6), same autonomous-overnight mode as modules 1-5 — executed directly by a forked sub-agent against ADR-022, which the orchestrator wrote and self-approved after reviewing module 5's clean completion (including its own Recruitment retrofit for Staffing Plan). Branch `feat/training-skills` stacked on `feat/employee-career-events`; this fork was itself interrupted once by the account's monthly spend limit mid-task (resolved by the user enabling usage credits) and resumed cleanly from the working tree with no rework. **The central design call**: source's `Training Result`/`Training Result Employee` exist mainly to be a submittable wrapper around per-attendee scoring, gated on the Training Event already being submitted — a docstatus ceremony this project doesn't have (ADR-016). Folded directly into `TrainingEvent.employees[]` rows instead; `markCompleted`/`markScheduled` actions reproduce source's real `on_update_after_submit` cascade explicitly. `EmployeeTraining` (a per-Employee "trainings attended" table) dropped entirely — source's own spec couldn't confirm which doctype embeds it, and it's fully redundant with querying `TrainingEvent.employees.employeeId`. **Three retrofits into modules 1 and 3, closing the Skill Assessment half of `OPEN-QUESTIONS.md` Q-9**: `Designation.skills[]`, `InterviewType.expectedSkillSet[]`, `InterviewFeedback.skillAssessment[]` — each explicitly deferred by its own module pending `Skill`'s existence. **Built**: `TrainingProgram`, `TrainingEvent`, `TrainingFeedback`, `Skill`, `EmployeeSkillMap` — 5 new models. New "Training & Skills" menu group (5 screens, reusing modules 4-5's `SimpleArrayField`/`SimpleActionButton`), 6 starter Skills seeded, 1 `widgetSources.js` entry (skipped one for `employee-skill-maps` — a lookup screen, nothing to group), manifest entries for all 5 screens. **Two real bugs found live wiring the retrofits in, both fixed, both their own commits**: `Designation`'s `updateDesignation` (module 1) overwrote unrelated fields with `undefined` on any partial update — including every `skills[]`-only update this retrofit needed — failing required-field validation as an opaque 500; `InterviewType`'s create/update and `InterviewFeedback`'s create (module 3) still destructured their pre-retrofit field lists, so the new array fields silently never saved even though the schema had room for them. Both caught by actually exercising the retrofits over real HTTP, not by reading the diff. **A third, unrelated pre-existing bug surfaced and worked around**: two reused throwaway-account emails collided with orphaned `LoginAttempt` rows an earlier module's verify session left behind (auth's login-attempt upsert isn't idempotent against a stale row for a deleted User) — cleaned up the specific stale rows after confirming they were orphaned, recorded as `OPEN-QUESTIONS.md` Q-12 so the next module doesn't rediscover it blind. **Verified live**: `npm test` 10/10, `npm run seed` twice (idempotent — 6 Skills both times), `npm run build` green, and a full HTTP walk covering every guard (endTime>startTime, all three Training Feedback guards, both markCompleted/markScheduled cascades, the duplicate-EmployeeSkillMap guard, `populateFromDesignation` pulling real skills with a deliberate mid-value proficiency default) plus **both Recruitment retrofits confirmed end to end** via a real Interview/InterviewFeedback chain (a throwaway Interviewer `User` had to be created through the full Country→State→City→User chain, same `OPEN-QUESTIONS.md` Q-10 gap reproduced again) plus role-permission checks with throwaway accounts, all cleaned up, employee count back to 195, the test employee's designation restored via a final `npm run seed`. | Module 7, **Travel** — check its real per-doctype specs before designing, same pattern modules 2/4/5/6 all hit; the Day-1 sketch (Travel Request, Itinerary, Costing, Purpose of Travel, Identification Document Type) is a guess, not a plan — Identification Document Type was already flagged (ADR-018) as belonging here. When a remote is available: push all six `feat/*` branches, decide the PR structure (stacked vs. squashed), and set up `gh` issues as the task ledger. Q-12 (LoginAttempt collisions on reused throwaway emails) is worth a real fix before too many more modules hit it blind. |
| 2026-09-10 | User provided a remote (`https://github.com/aumpatel2624/hrms-main-code.git`) and asked to push, merge, and clean up branches, plus run the branch setup. Ran the remaining `team-setup` branch-setup step (development/staging/production pushed to origin, default branch set to `development`). Pushed and merged modules 1-5's `feat/*` branches into `development`, in order, then deleted each. **A real snag, fixed, worth recording**: module 1's PR was squash-merged (`gh pr merge --squash`), which rewrites history — every subsequent stacked branch (2-5) then showed as "CONFLICTING" against `development` on GitHub, even though nothing semantically conflicted (each branch's content was always a strict superset of the previous: module-N-content = module-(N-1)-content + module-N's own additions). Diagnosed by checking a real merge attempt in an isolated git worktree (never touching the main working directory, where module 6's fork was actively running on `feat/training-skills` at the time) rather than guessing; confirmed the superset property file-by-file before resolving every conflict by taking the incoming branch's side, ran the full test suite in that worktree before each commit, and pushed as ordinary (non-force) merge commits — GitHub auto-detected each as satisfying its PR. Modules 3-5 then merged with zero conflicts (a real merge commit, unlike squash, preserves the ancestry the next stacked branch needs). All five `feat/*` branches deleted, both remote and local; `git fetch --prune` confirmed the local branch list matches remote exactly. `production`/`staging` deliberately left at the initial commit — promoting them is a human decision, not run here. Module 6 (`feat/training-skills`) was mid-build by its own fork throughout this and was not touched; it gets the same push/merge/cleanup treatment once it finishes. | Once module 6 finishes: push `feat/training-skills`, merge it the same way (a real merge, not squash, now that `development`'s history is clean again), delete the branch. From module 7 on, each module should push+merge into `development` as its own last step rather than accumulating unmerged branches, now that a remote and a working merge process both exist. |
| 2026-09-10 | Full pipeline for Employee Career Events (module 5), same autonomous-overnight mode as modules 1-4 — executed directly by a forked sub-agent against ADR-021, which the orchestrator wrote and self-approved after reviewing module 4's clean completion (including its general `auditPlugin` fix). Branch `feat/employee-career-events` stacked on `feat/onboarding-separation` (still no remote). **The central design call**: source's Transfer/Promotion apply changes via a generic `{fieldname, current, new}` row and Python `setattr` onto the Employee doc — source's own Port Notes flag the exclusion list protecting fields like `status`/`ctc` as client-only, not server-enforced, i.e. a real gap in the app being ported. Replaced with explicit typed fields (department/designation/branch for Transfer; department/designation/grade/CTC for Promotion) applied by plain assignment, logged to a new `EmployeePropertyChange` collection — narrower and safer than the mechanism it replaces, not a literal port of it. **Staffing Plan lost its entire parent/subsidiary-company validation layer** — source assumes a nested-set Company tree this project's flat, multi-company `Company` doesn't have; kept only the same-company overlap guard and the live-computed count fields. **Three source bugs in Employee Referral fixed, not reproduced** (status force-resetting to Pending on every save, a fetch_from pointing at a nonexistent field, an unbound-variable crash in a bonus-payout method) — ADR-016 already committed this project to working behavior over bug-for-bug fidelity, and there's no existing installation's data to stay compatible with. **Built**: `GrievanceType`, `EmployeeGrievance`, `EmployeeTransfer`, `EmployeePromotion`, `EmployeePropertyChange`, `EmployeeReferral`, `StaffingPlan` — 7 new models; `Employee` gained `ctc` (a plain number, not a Payroll concept). **The Recruitment retrofit, closing `OPEN-QUESTIONS.md` Q-8**: `jobOpening.controller.js`/`jobOffer.controller.js` (module 3) now check an active Staffing Plan's `numberOfPositions` against live usage before allowing a new Open posting or offer — its own commit, touching module 3's files. New "Employee Career Events" menu group (6 screens), 6 entity configs (Staffing Plan reused module 4's `SimpleArrayField`/`SimpleActionButton` components), 2 `widgetSources.js` entries, manifest entries for all 6 screens. **Verified live**: `npm test` 10/10, `npm run seed` twice (idempotent — 5 Grievance Types both times), `npm run build` green, and a full HTTP walk covering every guard (Transfer/Promotion applying real field changes + logging history, the Inactive-employee guard, the referral duplicate-email guard and the department-fetch bug fix, both Grievance conditional-required guards, the Staffing Plan overlap guard) plus **the Recruitment retrofit confirmed end to end** — a 2-position-capped plan allowed one Open Job Opening, rejected a second, and rejected a Job Offer for the same exhausted designation, all citing the plan by id — plus role-permission checks with throwaway accounts (HR User create-but-not-delete on Transfer, Employee role full access to Grievance but 403 on Referral), all cleaned up, employee count back to 195. **Three deviations found while building** (`DECISIONS.md` ADR-021 As-built note): `EmployeeReferral.email`'s uniqueness couldn't be scoped to non-Cancelled rows at the index level (MongoDB partial indexes don't support `$ne`) — fell back to a global unique index; `widgetSources.js`'s `aggregatable` shape was written as an object first, caught by checking the actual UI consumer before commit and fixed to the plain-string shape the doc comment actually specifies; `Employee.ctc` is only writable via the Promotion controller, not the generic Employee edit endpoint (module 2 predates the field). **Found, not fixed**: `POST /cities` leaks a raw Mongoose error on a 500, pre-existing and unrelated to HRMS (`OPEN-QUESTIONS.md` Q-11); `OPEN-QUESTIONS.md` Q-10 (no Country/State/City/User seed data anywhere in this project) reproduced again. | Module 6, **Training & Skills** — check its real per-doctype specs before designing, same pattern modules 2/4/5 all hit; the Day-1 sketch (Program, Event, Result, Feedback, Skill, Designation Skill, Expected Skill Set, Employee Skill Map, Skill Assessment) is a guess, not a plan. Once built, Interview Feedback's `Skill Assessment` child table (deferred, `OPEN-QUESTIONS.md` Q-9) needs a Recruitment follow-up touch. When a remote is available: push all five `feat/*` branches, decide the PR structure (stacked vs. squashed), and set up `gh` issues as the task ledger. |

| 2026-09-10 | Full pipeline for Onboarding & Separation (module 4), same autonomous-overnight mode as modules 1-3 — executed directly by a forked sub-agent against ADR-020, which the orchestrator wrote and self-approved after reviewing module 3's clean completion. Branch `feat/onboarding-separation` stacked on `feat/recruitment` (still no remote). **The central design call**: source models Onboarding/Separation as core-ERPNext `Project`+`Task` (not present here, and building it to serve one HR feature would be exactly the framework-fidelity work ADR-016 already ruled out) — replaced with `activities` embedded directly on each parent, each row carrying its own status, `boardingStatus` derived from them rather than from a linked Project's percent-complete. **Full and Final Statement got the second-largest scope cut in the project so far**: source drives its `status` entirely off a linked Journal Entry and auto-populates from Salary Slip/Gratuity/Leave Encashment/Asset Movement/a separate Lending app — none of that exists (no GL, per ADR-016 Q-3, now extended to cover this doctype too) and none of those source modules are built; simplified to a manual worksheet HR fills in by hand, with server-computed totals and a real settlement guard still gating `markAsPaid`. **Built**: `EmployeeOnboarding`(+`Template`), `EmployeeSeparation`(+`Template`), `ExitInterview`, `FullAndFinalStatement` — 6 new models, `EmployeeBoardingActivity` as a shared embedded shape (not its own collection). Real guards reproduced: duplicate-onboarding-per-applicant and duplicate-separation-per-employee (the latter a deliberate improvement over a gap source itself flagged, not silently copied from nothing), relieving-date-must-be-set on Exit Interview/F&F Statement, the F&F settlement guard (every line Settled, every returned asset Returned) before `markAsPaid`, `makeEmployee` blocked until every required-for-hire activity is Completed. Server-side template-to-record activity copying (source only did this client-side). New "Onboarding & Separation" menu group (6 screens), 6 entity configs, 2 new reusable admin components (`SimpleArrayField`, `SimpleActionButton` — this admin had no repeatable-row or action-button precedent at all; module 3's `JobOffer.offerTerms` and `JobRequisition`'s unwired `makeJobOpeningFromRequisition` import both left this gap for module 4 to actually fill rather than punt again), 2 `widgetSources.js` entries, manifest entries for all 6 screens. **A real, general bug found live and fixed in shipped starter infrastructure, its own commit**: `models/auditPlugin.js`'s global `mongoose.plugin()` reaches embedded subdocument schemas too, and its guard didn't exclude them — modifying an *existing* document's array-of-subdocuments field and re-saving crashed (`this.constructor.findById is not a function`, since a subdocument's `.constructor` isn't a real Model); creating one fresh never hit it. Fixed with a `$isSubdocument` guard on both the pre- and post-save hooks — this protects every current and future embedded-array model project-wide, not just this module's two. **Verified live**: `npm test` 10/10, `npm run seed` twice (idempotent, no real data for this module — same as Recruitment, only the menu group and 12 role grants), `npm run build` green, and a full HTTP walk against the real dev database covering every guard and both action flows (Onboarding→`makeEmployee`, F&F Statement settlement→`markAsPaid`) plus the role-permission asymmetries (several screens here are NOT the usual full-HR-User-and-HR-Manager pattern — neither role can delete Onboarding/Separation, Separation Template and Exit Interview give HR User read-only, F&F Statement is the one screen where HR User also gets delete — all confirmed live with throwaway accounts, then cleaned up, employee count back to 195). **Known, named simplification**: action endpoints (`markAsCompleted`/`makeEmployee`/`markAsPaid`) are gated on this starter's existing "edit" permission key, not a dedicated "submit" dimension — source restricts some of these to HR Manager alone; here HR User can also call them wherever HR User has edit rights on the underlying screen. | Module 5 — check `STATE.md`'s current "Employee Career Events" one-line description (Transfer, Promotion, Employee Property History, Referral, Grievance+Type, Staffing Plan+Detail) against the real per-doctype specs before designing, the same way modules 2 and 4 both found real scope corrections by doing exactly that. When a remote is available: push all four `feat/*` branches, decide the PR structure (4 stacked vs. squashed), and set up `gh` issues as the task ledger. |
| 2026-09-10 | Full pipeline for Recruitment (module 3), same autonomous-overnight mode as modules 1-2 below — executed directly by a forked sub-agent (not the parent orchestrator) against ADR-019, which the orchestrator wrote and self-approved first after reviewing module 2's clean completion. Branch `feat/recruitment` stacked on `feat/employee-records` (still no remote). **The scope cut**: this is the first HRMS module where Frappe's framework-fidelity machinery shows up on doctypes that matter (Interview/Interview Feedback/Job Offer are all `is_submittable` in source) — ADR-019 spells out, doctype by doctype, what got dropped per ADR-016 (no docstatus — each keeps only its own status field; no naming series; no scheduled jobs — interview reminders and the daily auto-close job are both gone, the latter replaced by a read-time query filter instead of a stored-status flip; no Kanban/print/letterhead/Terms-template/telemetry) and what got explicitly deferred rather than half-built (Staffing Plan vacancy checks and Employee Referral sync — both modules 5; Skill Assessment ratings — module 6; all three `OPEN-QUESTIONS.md` Q-8/Q-9). **The one real judgment call**: the public job board (user said "needed" during grilling) is inherently a *listing*, which conflicts with `30-api.md`'s public-endpoint gate ("resolve-by-key, never a listing") — built anyway as a second deliberate exception (ADR-004 precedent), but scoped to browsing only: the public *apply* flow (anonymous Job Applicant creation) was NOT built, because this starter has zero HTTP rate limiting and an anonymous write endpoint is a real abuse surface — flagged for the user rather than built silently overnight (`OPEN-QUESTIONS.md` Q-7). **Built**: `JobRequisition`, `JobOpening`, `JobApplicant`(+`JobApplicantSource`), `InterviewType`, `Interview`(+`InterviewFeedback`), `JobOffer`(+`JobOfferTermTemplate`) — 9 new models; `Employee.jobApplicantId` (optional) added as the integration point. Real guards from source reproduced as plain application-code validation (closed-opening/duplicate-application on Job Applicant, duplicate-interview-per-type/designation-mismatch on Interview, interviewer-assignment/date/duplicate on Interview Feedback, duplicate-offer on Job Offer) plus the cross-doctype syncs (closing an opening marks its requisition Filled; a Job Offer's status change syncs its Job Applicant; **the actual Recruitment↔Employee Records integration** — creating an Employee with `jobApplicantId` set flips both the applicant and its open offer to Accepted, mirroring source's own `after_insert` hook but as a plain controller call). Public listing (`/api/v1/public/jobs`) built as its own bespoke router, deliberately not on `runListQuery`. Role matrix extended — HR User/HR Manager full CRUD everywhere except Interview Feedback (**both read-only** there, only `Interviewer` writes — the second asymmetric-permission screen in this project, matching source exactly). New "Recruitment" menu group (9 screens), 9 entity configs, 3 grouped API wrapper files (`interviewers`/`defaultInterviewers` User-ref arrays are schema-ready but not exposed on the forms yet — no multi-select precedent in this admin), 2 `widgetSources.js` entries, manifest entries for all 9 screens. **A real bug found and fixed during verify, own commit**: the public listing's lookup-resolution helper stringified `null` refs before filtering, producing the literal string `"null"` in a Mongoose `$in` ObjectId query — 500 whenever a posting had an unset optional field (employment type, branch, ...). Found by actually hitting the endpoint with a realistic posting, not by reading the code. **Verified live**: `npm test` 10/10, `npm run build` green before and after, `npm run seed` run twice (idempotent, 5 Job Applicant Sources both times), and a full HTTP walk of the entire funnel against the real dev database — requisition → mapped opening → published → confirmed visible (and correctly field-allowlisted, salary hidden) on the **unauthenticated** public endpoint → applicant created → closed-opening guard (409) and the requisition-Filled cascade both confirmed → interview duplicate-type guard (409) → feedback: non-assigned interviewer rejected (403), assigned interviewer accepted, duplicate rejected (409) → offer duplicate guard (409), Accepted status synced the applicant → `makeEmployee` mapping → a real Employee created, reverse hook confirmed flipping both applicant and offer to Accepted → HR User confirmed read-only on Interview Feedback (200 GET / 403 PUT), Employee-role confirmed 403 on Job Applicant. This dev DB had zero `User`/`Country`/`State`/`City` rows seeded (the starter-generic `User` model needs all three, unrelated to HRMS) — created throwaway ones to run the role-matrix checks, all cleaned up afterward along with every other piece of test data; collection counts confirmed back to baseline (195 employees, 5 sources, zero elsewhere). | Module 4 — check `STATE.md`'s current one-line description against the real per-doctype specs before designing (Onboarding & Separation, or whatever it corrects to) the same way module 2's did; don't assume the Day-1 sketch is still right. When a remote is available: push `feat/organization-setup`/`feat/employee-records`/`feat/recruitment`, decide the PR structure (3 stacked vs. squashed), and set up `gh` issues as the task ledger. |
| 2026-09-10 | Full pipeline for Employee Records (module 2), same autonomous-overnight mode as module 1 below — this entry was executed directly (not via a separate forked sub-agent this time; the parent session's own fork-continuation ran the build itself) against a new ADR-018 the same session wrote first. **Scope correction made before any code**: the original module-2 one-liner (Employee core, Property History, Health Insurance, ID Document Type, Department Approver) was wrong — reading the per-doctype specs showed Property History/ID Document Type/Department Approver each have zero consumers until modules 5/6/8 respectively exist, so they moved there (ADR-018, `STATE.md`'s own module-list rows corrected in the same commit) rather than being built as orphaned infrastructure. Module 2 shrank to Employee + Employee Health Insurance. Branch stacked on top of `feat/organization-setup` (no remote yet, so no PR to target — noted as a real gap: once a remote exists, the user needs to decide whether these become two separate stacked PRs or one). **Built**: `Employee` (new — `employeeCode` globally unique from real org-chart codes, 4 required refs, self-referential optional `reportsToId`, `userId` optional/nullable for future self-service, several optional fields — `employmentTypeId`/`gradeId`/three approver refs/health-insurance fields/`shiftPreference`/`workMode` — added now so later modules don't need an Employee schema change, matching Employee Core Model Part A), `EmployeeHealthInsurance` (new, simple master), `employee.controller.js`/`.routes.js` (own file, with `$lookup` stages so the list shows names not ObjectIds), Employee Health Insurance CRUD folded into module 1's grouped `organizationSetup.controller.js`, real Apidel employee data seeded two-pass from `apidel-org-chart.csv` (195 employees, 193 reports-to links — matches 195 minus the 2 rows with no manager), role/permission matrix extended (HR User + HR Manager get Employee; Employee Health Insurance is HR-Manager-full/**HR-User-read-only**, the first asymmetric split — matches the Frappe source exactly), a new "HR Core" menu group (kept separate from module 1's "HR Setup" — Employee is the master, not configuration), `employeeConfig` (advanced tier, 8 lookup selects) and `employeeHealthInsuranceConfig` (uniform tier), two `widgetSources.js` entries, `docs-src/manifest.js` entries for both screens. **A real bug found by spot-checking, not by trusting the seed's own success counts**: the org-chart CSV has Windows line endings (Python's `csv.writer` default) — the seed's CSV parser split only on `\n`, so `gender` (the last column) was keyed as `"gender\r"` and every employee got `gender: null` on the first run despite the CSV having real values for all 195 rows. The create/reports-to-link counts looked completely correct; only checking three known employees against the raw CSV caught it. Fixed by normalizing line endings before parsing; re-verified with a full gender-distribution count (112 Male / 83 Female / 0 null). **Verified live**: `npm test` 10/10, `npm run seed` run twice (idempotent — 0 created/195 updated on the second run, same 193 links), spot-checks against known CSV rows (the 2 top-of-hierarchy employees confirmed `reportsToId: null`, date parsing confirmed correct), a real HTTP session with throwaway HR User and Employee-role accounts (missing-required-fields → 400, valid create → 201, **deleting a Department that now has an Employee → 409 — the cross-module regression check, confirming module 1's delete-guard picked up module 2's new ref automatically**, HR User's read-only-on-Employee-Health-Insurance confirmed, Employee-role 403'd on both new screens but still allowed through the matrix-free dropdown GET), `npm run build` green, all throwaway data cleaned up (Employee count back to 195). **Not done**: `npm run docs` screenshot capture (same gap module 1 left); `Employee.userId` self-service login provisioning has no UI yet (deliberate — bulk-creating 195 real people's credentials was explicitly rejected in ADR-018); the per-screen/company-confinement scoping mechanism and Department Approver both stay deferred to Leaves (module 8), now explicitly, not vaguely "module 2." | Module 3, **Recruitment** (Job Requisition → Opening → Applicant → Interview → Offer, plus the public job board — user confirmed this in scope during grilling). Read the per-doctype specs under `HRMS-Port-Spec/01-Modules/Recruitment/` before designing (not yet read in detail — only the module `_Overview.md`). When a remote is available: push both `feat/organization-setup` and `feat/employee-records`, decide the PR structure (stacked vs. squashed), and set up `gh` issues as the task ledger going forward. |
| 2026-09-10 | Full pipeline (`system-design` through `verify`/`update-docs`) for Organization Setup, run autonomously overnight per user instruction ("continuously run the pipeline... I won't be able to answer anything for now") — a forked sub-agent executed the build against ADR-017's design, which the orchestrating session wrote and self-approved under that standing authorization (see the ADR for why: user unreachable, and 60-limits.md's "decide before you build" question 1 needed an answer before module 1 could start). Environment bootstrapped first: this directory had never been `git init`'d — done locally (branches `development`/`staging`/`production`, guard hook confirmed blocking direct commits on them), `gh` installed and authenticated by the user mid-session but **no remote configured yet** ("make a git repo locally for now, I will give you the remote later") — so this module's commits are local-only, no push, no PR, no GitHub Issues ledger (STATE.md remains the task ledger until a remote exists). Also stood up a local MongoDB (portable binary, no root, `mongodb://127.0.0.1:27017/apidel-hrms`) since none existed, and ran `npm install` (approving the `bcrypt`/`esbuild`/`sharp` native-build install-scripts the default policy blocks). **Built**: `Company`/`Branch`/`Designation`/`EmploymentType`/`EmployeeGrade` (new models), `Department` extended with `companyId` (uniqueness re-scoped per-company, `departmentCode` made optional — real Apidel data has no code concept), a pre-existing `isOk`-on-400 bug in `department.controller.js` fixed in its own commit, `organizationSetup.controller.js`/`.routes.js` (6 endpoints × 5 resources), real Apidel org data seeded from `apidel-org-chart.csv` (1 company, 12 branches, 24 departments, 29 designations, 4 starter employment types), `RoleMaster`/`UserRoles` for the 6 non-admin HRMS roles with HR User/HR Manager granted this module's 6 screens and the other four roles deliberately given none, a new "HR Setup" menu group (the existing `/department` row moved into it in place, same `_id`, so no permissions were silently dropped), six admin entity configs (three moved to the advanced tier for a `companyId` lookup), six `widgetSources.js` entries, and `docs-src/manifest.js` entries for all six screens. **Verified live, not just by inspection**: `npm test` 10/10, `npm run seed` run twice confirming idempotency (identical counts both runs), `npm run build` green, and a real HTTP session against the actual dev database — admin login, Company/Branch create, the 409 delete-guard, Department's `companyId` requirement (400/201), and the permission matrix itself with two throwaway test accounts (HR User got 200/201 everywhere, Employee got 403 on the guarded routes but 200 on the matrix-free dropdown GET) — all throwaway data deleted afterward, counts confirmed back to baseline. Three deviations from ADR-017's original plan, each found by actually running something rather than by inspection, recorded in the ADR's As-built note: `departmentCode` optional not required; a `sparse`+`partialFilterExpression` index conflict with the soft-delete plugin, fixed with a custom partial filter; the backfill ended up as functions inside `seed/index.js` (matching this repo's actual existing convention for EmailFor/EmailTemplate) rather than a standalone script. **Not done**: `npm run docs`'s actual Playwright screenshot capture (manifest entries exist, fingerprint test passes, but no screenshots were generated); the per-screen/company-confinement scoping mechanism (`OPEN-QUESTIONS.md` Q-4) remains open, deliberately deferred to module 2. | Module 2, **Employee Records** (Employee core model, Property History, Health Insurance, ID Document Type, Department Approver) — nothing else in the HRMS build works without Employee existing. When a remote is available: push `feat/organization-setup`, open the PR (target `development`), and set up `gh` issues as the task ledger going forward. Two open items worth a look when the user is back: `npm run docs` screenshot capture, and whether the per-company scoping mechanism should be designed now or genuinely wait for module 2. |
| 2026-09-10 | `grill-me` run on `docs/knowledge/input/hrms/` (user request: "grill me for the hrms in the input folder"). First real client-domain work in this repo — everything before was generic starter infrastructure. Input is a full reverse-engineering of Frappe HRMS: 156 doctypes / 10 modules, documented at two depths (`HRMS-Obsidian-Vault/` narrative, `HRMS-Port-Spec/` mechanical build spec), plus `Apidel Organization Chart(1).html`. Read the overview/cross-cutting layer of both trees in full (see the Input documents table above for exactly which files); the 156 per-doctype detail files are deliberately not yet read — deferred to each module's own `system-design`/`schema-design`, per "build one module at a time." Recovered real org data from the org-chart file (it's a Claude Design Canvas export, data bundled as opaque JS, not plain HTML — required extracting an embedded `__bundler/template` → `text/x-dc` script, not a simple read) — 195 employees, 25 departments, 29 designations, a manager hierarchy, saved to `docs/knowledge/apidel-org-chart.csv`. Flagged the scope reality per AGENTS.md rule 9 before asking anything: a literal Frappe port assumes framework primitives this starter doesn't have (Draft/Submit/Cancel/Amend document lifecycle, naming-series numbering, a background job scheduler, a GL accounting engine, a `Company` model) — asked the user to choose fidelity rather than picking silently. Worked the frontier across four rounds, every answer the recommended one **except** two: **(1) fidelity — idiomatic rebuild** (status fields, no docstatus/GL, background jobs only where a feature can't work without one); **(2) multi-company — user chose multiple** (not the recommended single-company default) — `Company`/`Branch` become first-class models here, built here, since nothing else supplies them; **(3) company confinement — confined by default**, System-Manager-equivalent sees across companies; **(4) public job board — user chose needed** (not the recommended "not in scope") — Recruitment's public `/jobs` route is in scope; **(5) per-screen data scoping — extend** ADR-002's per-role `dataScope` to per-screen, needed for the self/approver/global role-shape split; **(6) data retention — starter default**, keep indefinitely; **(7) language — single, English**. Recorded as ADR-016 (`DECISIONS.md`), PRD.md scope section (new "HRMS (Apidel)" in-scope block + five new out-of-scope entries), DOMAIN.md (HRMS vocabulary + spine-entity summary for Company/Branch/Department/Designation/Employee — full per-entity depth deferred per module), RULES.md (INV-8, PERM-1, two new Deviations rows DEV-1/DEV-2), OPEN-QUESTIONS.md (Q-3 through Q-6: payment reconciliation without a GL, the per-screen-scoping mechanism itself, the background-job scheduler technology choice, and a real data-cleanup question — two near-duplicate department names in the org-chart CSV). Module list (17 modules) added to this board below, in dependency order derived from `HRMS-Port-Spec`'s own Build Order doc but re-split and reordered for Company/Branch-first. Module order and the department-name normalization were then both confirmed by the user in a second pass (both recommended options taken; see Q-6's answer column). | `system-design` on the first module, Organization Setup (Company, Branch, Department, Designation, Employment Type, Employee Grade, HR Settings) — nothing else in the HRMS build can start without it. |
| 2026-09-09 | `client-docs` for Email trigger system. No new screen — `EmailFor`/`EmailTemplate` already had documentation pages from the original client-docs module; only their forms changed, so this updated their prose and recaptured their screenshots. `docs-src/manifest.js`: `email-for` gained a gotcha on the trigger dropdown going empty (all events already claimed) and a second, more serious one — a row whose Trigger column reads `unassigned.*` (a pre-existing-purpose backfilled by `backfillEmailForTriggerKeys`) cannot currently be edited safely (see finding below); `email-template` gained gotchas on the merge-field tokens and the one-active-template-per-occasion guard. `apps/server/seed/fixtures.js`: the three demo `EmailFor` rows now carry real `triggerKey`s (one real — `password.forgot`, wired to real `{{USERNAME}}`/`{{OTP_CODE}}` content — two `unassigned.*` placeholders) since the field is now required; without this the fixture seed itself failed validation. **Four infrastructure bugs found and fixed in `scripts/docs-capture.js`/`apps/admin/vite.config.js`, none specific to this module but all blocking this run on this machine**: (1) `spawn("npm", …)` threw ENOENT/EINVAL on Windows — needs `shell: true`, gated behind a new `ON_WINDOWS` check; (2) Vite's `--host` default resolved to the IPv6 loopback only, leaving the hardcoded IPv4 `ADMIN_URL` unreachable — fixed with an explicit `--host 127.0.0.1`; (3) the Playwright login step clicked two consent checkboxes by label that no longer exist (removed from `Login.jsx` in a prior session; the server-required flags are now sent unconditionally) — the dead click step was deleted; (4) `shutdown()`'s process-group kill via a negative PID is POSIX-only, so a failed Windows run left orphaned server/admin processes holding their ports — now uses `taskkill /T /F` on Windows. **One real screenshot-quality bug found by actually looking at the captures** (not by the run reporting success): `capture()` used `fullPage: false`, so any form taller than the ~1000px viewport was silently clipped at the bottom — invisible until the new Merge fields panel pushed the Email Template form past that line and the whole panel vanished from the add/edit screenshots. Switched to `fullPage: true` (forced a full `--force` recapture of all 128 images, not just this module's). That in turn exposed a second, subtler bug: a `position: sticky` footer (`FormActions`) painted an opaque bar over content that reflowed into its old viewport-relative offset during the resize-before-shoot fullPage capture does internally — silently erasing just the "Merge fields" heading, on both the add and edit forms, in a spot too easy to miss without zooming in. Fixed by forcing `.sticky { position: static }` in the same injected style tag that already freezes animations for capture. Also fixed, unrelated to any of the above: Vite's `server.open: true` (for a human's normal `npm run dev`) was popping a new visible browser tab on every capture run since `docs-capture.js` restarts that dev server each time — gated behind a `DOCS_CAPTURE` env var `docs-capture.js` now sets, so normal dev is unaffected. **A real, live product bug found and NOT fixed here** (out of scope for this module, logged as `OPEN-QUESTIONS.md` Q-2): the `EmailFor` edit form's Trigger dropdown is built only from the code registry, so it can never display or preserve an `unassigned.*` value — two real rows in the dev database ("Contact Enquiry", "test") cannot currently be edited without either failing validation or being silently reassigned to a different real trigger. User chose to document this honestly (done, in the new gotcha above) and fix separately rather than pause here. Non-admin, both-themes check done: logged into the panel as a throwaway role/user, opened `/documentation/email-for` and `/documentation/email-template` directly, confirmed both new gotchas render correctly in light and dark — including that the `/documentation` menu row, defined in `seed/index.js` since ADR-010, had never actually been seeded into this dev database (`npm run seed` fixed it; matches the seeded-Documentation-module's own "Human: npm run seed" follow-up that was apparently never done here). `npm test` (10/10) and `npm run build` both green after all script/config changes. All throwaway test data (roles, users, matrices) created during this session's `verify` and `client-docs` checks deleted afterward; two dev-server processes orphaned by Windows not cleanly killing `npm run dev`'s child tree were found and killed manually both times it happened. **Shipped**: two commits pushed straight to `main` (no `development`/`staging`/`production` branches exist in this repo, and `TEAM.md` is unfilled — user confirmed committing directly, matching how every other recent commit here has landed) — `96b5e38` (docs-capture infra fixes) and `edf41f5` (the actual client-docs content). | Module fully shipped. Two logged findings still need a human decision, both candidates for a future `fix-bug` pass: `OPEN-QUESTIONS.md` Q-1 (duplicate menu rows/groups for five URLs) and Q-2 (`unassigned.*` trigger edit trap on `EmailFor`). Otherwise: Reports or Analytics (both still `-` on the board) are next up, or `team-setup` if more than one person will work this repo. |
| 2026-09-08 | `update-docs` for Email trigger system. ADR-015 closed with an As-built note (the `password.reset` trigger, confirmed live behavior, the duplicate-menu-row finding flagged but not fixed there). `DOMAIN.md`/`RULES.md` already matched the shipped code exactly — checked, nothing to change. `CHECKLISTS.md`: the two remaining unchecked items (the `60-limits.md` line, and the OTP-send/400/409 acceptance checks) both ticked now that `verify` ran them. `60-limits.md`: removed the "Email has exactly one wired sender" row (closed gap) and added the trigger-registry capability to "What you do get" — this is the one Tier-1 file `update-docs` may touch routinely per its own skill file. `OPEN-QUESTIONS.md`: added Q-1 for the duplicate-menu-row bug found during `verify` (full detail in the 2026-09-08 `verify` log entry above) — a project-specific data-hygiene defect, not a starter limit, left open pending a decision on deduplicating the rows and re-pointing existing roles' matrices. `PRD.md` untouched — scope did not move. | `client-docs` (the original ask) — the module is fully verified and documented now. After that, `git-flow`. Separately, the user should decide whether the duplicate-menu-row bug (`OPEN-QUESTIONS.md` Q-1) becomes its own `fix-bug` task. |
| 2026-09-08 | `verify` for Email trigger system, run against the same dev database (`apps/server/.env`, previously confirmed safe). `npm test`: all 10 suites green including `sendTriggeredEmail.test.js`. `npm run build`: green. Exercised live over HTTP + Playwright against the real "Forgot Password"/"Reset Password" `EmailFor` rows and their active templates (created in the prior session, still live): OTP send through the trigger path actually delivered via the configured Gmail SMTP account (`Email sent successfully`, 200); the 400 undeclared-token rejection (INV-6) and the 409 duplicate-active-template rejection (INV-5) both confirmed with exact messages; the already-claimed-trigger 400 (INV-4 backend) also confirmed. **Found and worked around, not fixed, a pre-existing bug**: `/api/v1/menus` has duplicate active rows for five URLs — `/email-for`, `/email-template`, `/email-setup`, `/client`, `/project` — two each, an older set from 2026-08-05 and a newer set from 2026-08-21, same `menuGroup`, from some past non-idempotent reseed or migration. This breaks the *client's* menu-id-to-permission resolution (`MenuContext.findMenuIdByUrl`, first-match-wins) independently of the server, which resolves correctly — a role granted access via today's role editor (reading the current live menu tree) may silently not see write buttons if the client's tree-walk picks the stale duplicate. The real "test" role's matrix still only references the old 2026-08-05 ids. **Left untouched**: did not edit the real "test" role, the real `Dev Vaghela` user, or the duplicate menu rows — out of scope for this module and risky to improvise. Non-admin pass instead used a throwaway role granted on both old and new ids plus the group, and a throwaway user, both deleted afterward (the role's `UserRoles` doc had no delete route, so that and the role itself were removed directly via mongoose since the API's own delete-guard correctly blocked deleting a role still referenced by a matrix). Confirmed as non-admin, screenshots taken in both themes: Email For's list shows the Trigger column; the Add form's trigger dropdown correctly shows zero options (both real triggers already claimed, so nothing to offer — expected); Email Template's merge-field hint renders `{{USERNAME}}`/`{{OTP_CODE}}` for "Forgot Password" in light and dark. Diff re-read (`git show 0372a63`): the two `console.log(error)` / raw `error` pattern hits in the new code match six pre-existing instances already in the same file — not a regression, left as-is. No code changed this session; only throwaway DB test data, all cleaned up. | Report the duplicate-menu-row bug to the user (candidate for `fix-bug`, separate from this module). Then `update-docs` to close out ADR-015 and sync `DOMAIN.md`/`RULES.md`/`CHECKLISTS.md`/`OPEN-QUESTIONS.md`, then `client-docs` (the original ask), then `git-flow`. |
| 2026-09-07 | `new-page` for Email trigger system. No new screen/menu row — `EmailFor` and `EmailTemplate` already exist; this only changed their forms. `emailForConfig` (UNIFORM_ENTITIES, `entities/index.js`) gained a `triggerKey` select field (`Zap` icon) fed by a `triggerOptions` lookup that calls the loader signature `(values, { id, mode })` `crud-form.jsx` already provides — filters `GET /email-for/triggers` to entries with no claim or claimed by the record's own id, so editing a claimed row doesn't blank the field; same loader reused by `crud-view.jsx` for the view screen's label. `emailTemplateConfig` (ADVANCED_ENTITIES, `entities/advanced.jsx`) gained `renderExtra: ({ values }) => <EmailTemplateMergeFields values={values} />` — new component `components/crud/email-template-merge-fields.jsx`, same "documented hook" `PasswordResetSection` already uses. It deliberately calls `GET /email-for` (the list Email Template's own "Email For" dropdown already uses, unauthenticated-by-role per ADR-002's dropdown convention) rather than `GET /email-for/:id` (which is `checkPermission`-gated) — avoids a role that can open this screen but not Email For seeing the hint silently break. **Verified live**, not just built: `npm run seed` run against the dev database in `apps/server/.env` (user confirmed safe) — surfaced that this DB has no "Forget Password" row at all (it has "test" and "Contact Enquiry" instead), both correctly backfilled to placeholder `unassigned.*` keys, proving the backfill's fallback path on real messy data, not just the happy path. Logged into the already-running dev server (a pre-existing `nodemon` process that had been auto-reloading through every edit this session) via a throwaway Playwright script and confirmed, with screenshots: the trigger dropdown offered exactly "Forgot password" then emptied once a test `EmailFor` claimed it (INV-4 working end-to-end through the UI); the list screen's Trigger column showed the real keys including the backfilled placeholders; the merge-field hint on Email Template showed `{{USERNAME}}`/`{{OTP_CODE}}` for the claimed Email For, correct in both light and dark mode. Test data and the throwaway script were deleted afterward; the admin dev server process I started for this was stopped (the pre-existing server process on :7002 was left alone — not mine to touch). **Not exercised**: an actual OTP email send through the new path, and triggering the 400 (undeclared token) / 409 (duplicate active template) responses directly — `verify`'s job. | `verify`: exercise the OTP send end to end, the two rejection paths (undeclared token, duplicate active template), and as a non-admin role. Then close the "Email has exactly one wired sender" line in `60-limits.md`. |
| 2026-09-07 | `api-endpoint` for Email trigger system (ADR-015 built). `apps/server/config/emailTriggers.js` (registry, one entry: `password.forgot`); `apps/server/utils/sendTriggeredEmail.js` (`fillMergeFields`/`validateMergeTokens`/`extractTokens` pure + async `sendTriggeredEmail`, never throws, unit-tested in `sendTriggeredEmail.test.js`, added to `npm test`); `emailFor.controller.js` gained `validateTriggerKey` (registry membership + not-already-claimed) wired into create/update, and `listEmailTriggers` (new `GET /email-for/triggers`, registered before `/:emailForId` so Express doesn't swallow it as an id param); `emailTemplate.controller.js` gained `validateAgainstTrigger` (INV-6) and `findConflictingActiveTemplate` (INV-5) wired into create/update — both compute the *resulting* emailFor/subject/body/isActive so an update that omits a field is validated against what the record will actually read as, not just what was sent; `otp.controller.js`'s `createOtp` now calls `sendTriggeredEmail("password.forgot", ...)` and maps `reason` (`no_trigger`/`no_template`/`send_failed`) onto its original 404/404/500 responses — no caller-visible behaviour change, and it no longer leaks `error.message` in the 500 body (incidental fix, matches the "no raw error string in a response" convention). Swagger `EmailFor`/`CreateEmailFor` schemas corrected to the real field names while touched (they'd drifted: `emailForName` → `emailFor`, plus new `triggerKey`); `EmailTemplate`/`EmailSetup` swagger schemas left as their pre-existing drift, out of scope here. Admin client: `ENDPOINTS.EMAIL_FOR.TRIGGERS` + `getEmailTriggers()`. Kept the manual-validation style already in these three controllers rather than introducing `allowOnlyFields`/express-validator, per ADR-015's stated deviation. `widgetSources.js` untouched, per ADR-015. Verified: `npm test` 9/9 (new suite included), `npm run build` green. Not yet done: the admin screens (`EmailFor`'s trigger dropdown, `EmailTemplate`'s merge-field hint) and an actual HTTP/browser run of the new behaviour — both `new-page` and `verify` are still ahead. | `new-page`: wire the `triggerKey` dropdown into `emailForConfig` (fed by `getEmailTriggers`, filtering to unclaimed + the record's own current key) and show declared merge fields on `emailTemplateConfig`'s form. Then `verify`. |
| 2026-09-07 | `system-design` for Email trigger system: ADR-015 — a code registry (`apps/server/config/emailTriggers.js`, mirroring `widgetSources.js`/ADR-003) mapping trigger key to declared merge fields, `EmailFor` gains a `triggerKey` field (registry-validated, unique via the existing soft-delete partial-index mechanism), a new `GET /email-for/triggers` route (mirrors `GET /dashboard-widgets/sources`) serving one payload that feeds both the trigger dropdown on `EmailFor`'s form and the merge-field hint on `EmailTemplate`'s form, a partial-unique-index-backed "one active template per trigger" guard, save-time validation that a template's `{{TOKENS}}` are all declared, and `apps/server/utils/sendTriggeredEmail.js` (pure `fillMergeFields`/`validateMergeTokens` plus an async sender that never throws — returns `{sent, reason}`) replacing the inline lookup/replace/send block in `otp.controller.js`. No new collection, no new public route, no new dependency. Reporting phase explicitly skipped and flagged (not fixed): `EmailFor`/`EmailTemplate` aren't in `widgetSources.js` today and nobody has asked for trigger-firing stats — pre-existing gap, out of scope here. Brief shown to the user in-conversation; not yet explicitly re-confirmed after ADR-015 was written. | Get the brief/ADR confirmed, then `schema-design`: `EmailFor.triggerKey` + the `EmailTemplate` partial unique index. |
| 2026-09-07 | `grill-me` run on `docs/email-trigger-system.md` (user request: "start implementing"). Read the doc plus `60-limits.md`, the ADR log, the three email models and `otp.controller.js` before asking anything. Worked the doc's five open questions as the frontier, batched with one added scope question, across three rounds — every answer was the recommended option: **(1) scope** is infrastructure + the one real trigger site only, no new consumer form is invented (this repo has no public site to submit one from); **(2) one template per trigger**, no fan-out — a form needing two emails fires two trigger keys; **(3) `sendTriggeredEmail` never throws** on a missing/inactive template, it logs and returns a result the caller inspects (OTP can still choose to 404 on that result, matching today); **(4) merge fields declared in a code registry** (`apps/server/config/emailTriggers.js`, mirroring `widgetSources.js`), not the database; **(5) the trigger key lives on `EmailFor`** (a new `triggerKey` field) rather than a new collection; **(6) the duplicate-active-template gap the doc found gets closed in this module**, not deferred; **(7) `triggerKey` is picked from a registry-fed dropdown**, never typed freehand, so it can never point at nothing. Wrote all seven into `PRD.md` (scope item 2 + three out-of-scope entries), `DOMAIN.md` (EmailSetup/EmailFor/EmailTemplate entity blocks, trigger-key + merge-field vocabulary, relationships), `RULES.md` (INV-4 through INV-7), and a new per-module section in `CHECKLISTS.md`. Nothing left in `OPEN-QUESTIONS.md` — the frontier closed. No code written (writing code is out of scope for this skill). | Run `system-design` for this module: name the approach for the registry endpoint (new route vs. reusing a dropdown-source pattern), write the ADR, then `schema-design` for the `EmailFor.triggerKey` field and the two new validation rules. |

> Entries before 2026-08-22 cite `Q-n`/`A-n` rows in `OPEN-QUESTIONS.md`. Those were about the
> starter rather than any one project, and were consolidated into
> [docs/conventions/60-limits.md](../conventions/60-limits.md) on 2026-08-22 — the log is left
> as written.

| Date | What happened | Next |
|---|---|---|
| 2026-09-05 | Explained (not built) how email sending currently works — `EmailSetup`/`EmailFor`/`EmailTemplate` already make template *content* editable data, but the only real trigger site, forgot-password OTP in `apps/server/controllers/v1/otp.controller.js`, hardcodes its purpose lookup (`"Forget Password"`) and its two `{{TOKEN}}` replacements inline; a second form today would mean copy-pasting that block. Also found a real gap while reading it: nothing stops two active `EmailTemplate` rows sharing one `EmailFor`, so `otp.controller.js`'s unsorted `findOne` picks whichever Mongo returns first — already ambiguous, not just a future risk. Wrote up a proposed dynamic-trigger-key design (shared send function, one call per form instead of one copy of the lookup/replace/send block) and the open questions it raises (one template per trigger vs many, missing-template behaviour, where merge fields get declared) in `docs/email-trigger-system.md`. No `system-design` session run, no ADR, no code changed. | Run `system-design` for this module when the user is ready to build it — start from the open questions in `docs/email-trigger-system.md`. |
| 2026-09-01 | **CI/CD automation added on top of the existing manual deploy** (Certbot + Nginx + PM2 already running the app by hand on a Hostinger VPS). `.github/workflows/deploy.yml`: a `test` job (npm ci/test/build) on every push and PR, and a `deploy` job that runs only on a push to `main` (i.e. after a human merges) — it SSHes in and runs the *identical* `git pull --ff-only` → `npm ci` → `npm run build` → `pm2 restart --update-env` sequence already documented in `70-deployment.md`, then checks the pm2 process actually came back `online`. Secrets are passed through `env:` rather than interpolated into the script body (GitHub's own script-injection hardening guidance). `70-deployment.md` gained a "Continuous deployment" section: trigger rules, the one-time VPS-side key setup, the six required GitHub secrets, namespacing (one workflow + one secret set per app on a shared host), and a rollback note. Single environment only — `main` → production — since this repo has no `staging`/`development` branches yet and the user chose not to set those up now. **Not yet tested end-to-end**: no VPS access in this session, so the SSH step, the pm2-name/APP_DIR values and the deploy user's passwordless git/pm2 access are unverified — first real run will be the proof. **Not committed/pushed** — `git config user.email` (`dev.inbweb25@gmail.com`) has no row in `TEAM.md` (still "not set up yet"), so whose-agent-am-I is unresolved; also flagged that the standard `feat/<issue>-<slug>` branch-off-`development` flow doesn't apply since only `main` exists. | Human: run `team-setup` (or at least add a TEAM.md row) if PRs are wanted; add the 6 GitHub secrets; generate and install the CI deploy keypair on the VPS; then push this branch and watch the first `deploy` run. |
| 2026-08-29 | **Documentation section restyled on the panel's own tokens, and the operation sections illustrated** (ADR-014, superseding ADR-012). The Material for MkDocs reproduction is gone: the stylesheet is rebuilt on `--color-*` semantic tokens with no colour literals, so the section follows a rebrand instead of being pinned to Material's indigo, and the Roboto webfont is dropped from `index.html` (one fewer third-party request on every page). **The two stacked headers are now one** — `Documentation.jsx` portals the page-list toggle and search into `#app-header-slot`, a new empty slot the layout renders in its own header; shrinking the second bar had not been enough, the user asked for one header. Screenshots now illustrate the four operation sections: every config-driven screen is captured in `list`, `add`, `view` and `edit` (44 → 128 images), reached by clicking the row action because view/edit need a real record id. Both themes are still captured but only the reader's is shown, via a CSS rule rather than a render-time choice, so a theme toggle swaps every picture with no re-render. **Four bugs found and fixed, three of them pre-existing and shipping:** the CSS minifier rewrites `list-style: disc` to `list-style: outside`, so *every bullet in the documentation was invisible in production while correct in dev* (uses the `list-style-type` longhand now); a callout's last paragraph kept its bottom margin and left a stranded gap; the page-list toggle hid at Tailwind's 1280px while the sidebar turned permanent at 1220px, leaving a dead button in the gap; and photographing the edit routes exposed that **the Users edit form sent populated relation objects to the state/city lookups**, failing their ObjectId cast and leaving both dropdowns empty — a user's existing state and city were invisible and unchangeable (`refId` helper in `entities/advanced.jsx`). Also hardened `docs-capture.js`: it spawned its servers without a process group and killed only the npm wrapper, so a failed run orphaned a server that held port 5055 and made every later run hang — three runs were lost to this before it was diagnosed. Verified: `npm test` 9/9, build green, 128 images referenced and 128 present with no orphans, read in the running panel in both themes. | Commit and deploy — neither done yet; the server still runs `3d2f8ff`. Then backfill client docs for the six earlier modules, or start Reports. |
| 2026-08-29 | **Documentation pages now cover adding, viewing, editing and deleting** (ADR-013). The generated pages described what each screen *is* but never how to work it; all fifteen config-driven pages now carry four sections between the filter list and the gotchas. The steps are written once in `OPERATION_SECTIONS` in `scripts/docs-generate.js`, not per screen, because they come from the three shared components every CRUD screen renders through — so button labels are quoted exactly as the app renders them (`Add Department`, but `Create department`: `crud-list` capitalises the singular and `crud-form` lowercases it) and the test asserts them verbatim. Two manifest flags: `listOnly` emits only the viewing section (SEO Pages, which hands off to its own editor), `operations` overrides one section's body. Found and fixed a **pre-existing CSS bug** while checking the result in the browser: `documentation.css` styled `li::marker` but never set `list-style`, so Tailwind's Preflight reset had stripped the bullets from every list in the docs section — the new numbered steps just made it visible. `system-design`/`schema-design`/`api-endpoint` skipped with reason: documentation-only change to a shipped module. Verified: `npm test` 9/9 with new coverage for the sections, the `listOnly` case and the article/casing edges ("an SEO Page", "a User"); build green; `npm run docs` reports all 22 screenshots current (the fingerprint hashes each screen's own manifest entry, and only the file header changed); read the Departments and SEO Pages pages in the running panel. | Backfill client docs for the six earlier modules, or start Reports. |
| 2026-08-28 | **Client documentation shipped and deployed.** PR #17 (the module) and PR #18 (hooks warning when a screen is missing from `docs-src/manifest.js`, plus a write-guard on the generated pages and screenshots) both merged; `main` at `29feb71`. Deployed to `cms-demo` on vyaris.com at `/home/deployer/project/cms-demo` — **not** `/var/www/demo.vyaris.com`, which looks like the checkout and is not one; the host runs four PM2 apps, so the path and process name are worth confirming rather than guessing. `npm run seed` took the menu tree from 4 groups to 5 (Documentation). Verified from outside the box: HTTPS 200, the Material markup and prose present in the served bundle, and a screenshot asset returning 200 / 205 KB. The other three apps were untouched. Note for future deploys: **never run `npm run docs` on a server** — it seeds a fixture database and drives a browser; the screenshots are committed and the server only serves them. | Backfill client docs for the six earlier modules (Soft delete, Role scoping, Dashboards, SEO, Audit, Canvas) — the machinery exists now, so it is one pass. Or start Reports. |
| 2026-08-28 | **Client documentation built** (ADR-011, ADR-012). Four pieces: `apps/server/seed/fixtures.js` (demo data — 6 departments, 14 users, audit/SEO/dashboard rows; refuses any database not named `demo-panel-docs-fixtures`, guard tested), `scripts/docs-generate.js` (reads each entity config for structure, merges prose from `docs-src/manifest.js` — 23 pages), `scripts/docs-fingerprint.js` (incremental capture) and `scripts/docs-capture.js` (Playwright). New screen at `/documentation` + seeded top-level menu row. `schema-design` and `api-endpoint` skipped with reason: no collection, no endpoints — the pages are build-time assets. **Playwright was already a devDependency** with browsers cached, so only `marked` was added. Five real bugs found by running it, not by reading: LoginAttempt's `userEmail` is uniquely indexed (one row per user, not per attempt); the login route is `/` not `/login`; the consent checkboxes are react-aria, so `check({force:true})` sets the DOM without firing React and the login is silently rejected; **`window.confirm()` in the login handler** — Playwright auto-dismisses dialogs, so login aborted with no request, no error and no toast; and Recharts animates in JS, so the first pie chart was captured mid-sweep. Styling moved twice: built on the panel's tokens, then rebuilt as a faithful Material for MkDocs reproduction at the user's instruction after seeing it (ADR-012, a recorded deviation from 40-frontend.md). Verified: `npm test` 9/9, build green, **44 screenshots captured and visually checked**, incremental recapture demonstrated (2 changed, 20 reused). | Human: `npm run seed` on each database for the Documentation menu row. Then backfill docs for the six shipped modules, or start Reports. |
| 2026-08-28 | **Board correction.** SEO management and Audit trail were both marked `Shipped: -`; both are in fact merged to `main` — SEO as commit `db27699`, Audit alongside it, with PR #10 hardening it afterwards. Dashboard layout canvas likewise (PRs #11/#12). All PRs #2–#16 are merged, including #16 from the current branch. Rows corrected to `done`. The stale board had led me to sequence Client documentation behind an SEO/Audit merge that had already happened. Deployment state is not visible from git — confirmed by the user as deployed. | Client documentation is no longer blocked: `system-design`, starting with the screenshot fixture seed. |
| 2026-08-28 | Explored **client documentation** (`explore-idea`). Audience settled as the client's *end users*, not engineers — `docs/conventions/` already serves engineers. MkDocs Material (the original ask) declined: Python in a Node-only repo and deploy, and a standalone static site cannot be an ordinary menu row behind the session cookie; recorded in PRD out-of-scope. Chosen instead (ADR-010): markdown generated from each screen's entity config with agent-written prose in the same pass, rendered in-SPA at a normal route with a seeded menu row, illustrated by screenshots captured automatically by a browser driver. User chose full automation over manual capture, then asked for it to be **incremental** — each screen fingerprints its config/page file, the shared UI it renders through, the theme CSS, the fixture seed and its capture-list entry, and only recaptures when that hash moves (same mechanism as the input-document hash table above); `--force` recaptures all. Runs as its own `npm run docs`, not welded to `npm run build`, so an ordinary build needs neither MongoDB nor a browser. New `client-docs` skill at pipeline phase 7.5, after `update-docs`. Prerequisite is a screenshot fixture seed producing presentable demo data — `npm run seed` makes only a menu tree and one admin user, which photographs as empty tables. Two dependencies still to approve when reached: a browser driver (Playwright expected, downloads its own binaries) and an SPA markdown renderer. Docs for the six already-shipped modules deferred to a single backfill pass once the machinery exists. **Nothing built yet — exploration only.** | Merge SEO + Audit PRs first. Then `system-design` for Client documentation, starting with the fixture seed (nothing else can be built before it). Reports and Analytics follow. |
| 2026-08-22 | **Audit trail** (ADR-005), built to close the second *Decide before you build* question rather than leave it as a gap — `explore-idea` had been claiming the starter already had audit logging, which was false; now it is true. Global Mongoose plugin `models/auditPlugin.js` (same shape as softDelete, throws on boot if a model compiled first) + `utils/auditContext.js`, an `AsyncLocalStorage` store filled by one `app.use` after the session. **No actor, no row** — the seeder, the public SEO endpoints and the redirect hit counter write nothing, which is what keeps the collection readable. `utils/auditDiff.js` is pure (flatten to dot paths → diff → redact → classify) and unit-tested; it caught a real bug where a soft delete diffed to an empty change list and was therefore dropped, so deletes would never have been logged. `AuditLog` denormalises the actor so rows stay readable after an account is deleted. Endpoints are **read-only by design** — no create/update/delete route exists. UI: `pages/Master/AuditLog.jsx`, custom page (a CRUD config would offer edit and delete on an audit log), with filters, a diff modal, and secrets shown as "hidden". Seed menu row `/audit-log` under Master; seeder needed the plugin import added or its model-loading loop tripped the boot guard. Verified: `npm test` green (auditDiff added), build green, **35/35 HTTP acceptance + 7/7 permission-matrix** on a throwaway local db (dropped) — covering create/update/delete/no-op, password + hash never reaching the log, public traffic excluded, no self-audit, and that POST/PUT/DELETE on the log are not routes. Docs: ADR-005, 20-schema "The audit trail", 30-api "read-only", 10-architecture lifecycle + registry, 60-limits (audit moved from gap to capability; new limits recorded for retention, bulkWrite and hard deletes). **Not done: browser click-through / dark-mode pass.** Also fixed: my own regressions in `grill-me` from the previous session — a 118-char line and a dropped "audit and retention" domain question. | Human: `npm run seed` per database (new /audit-log menu row), visual pass of the Audit Log screen. Decide a retention policy before the collection gets large — it grows unbounded by design. Then module 3: Reports (system-design). |
| 2026-08-22 | **SEO management** built outside the pipeline at the user's instruction (module predates the skills/knowledgebase; docs backfilled afterwards, this entry included). ADR-004: path-keyed SEO for the site's *fixed* URLs only — dynamic detail pages will carry their own SEO fields on their own collections (Q-7/A-4), so no pattern engine, no shared field group, single language. Five collections (SeoPage, SeoSettings singleton keyed on a constant, SeoRedirect, SeoNotFound, SeoUrl). Resolver is **pure and lives in `packages/shared`** (`seo.js` + `seoResolve.js`) so the admin editor previews every keystroke locally with the identical code the server runs — no preview endpoint, no drift. Merge order: redirect → page → site defaults → constants → token interpolation → flat Helmet-ready tags. Server: `seo.controller.js` + `seoPublic.controller.js`, `seo.routes.js` (matrix-gated on /seo-pages, /seo-settings, /seo-redirects, /seo-404) and **`seoPublic.routes.js` — the first unauthenticated routes in the repo** (deviation, ADR-004; 30-api.md rewritten with a four-question gate for any future one). Public writes guarded by a generated site key (`middlewares/siteKey.js`, constant-time, 503 when unconfigured). `utils/seoCache.js` mirrors checkPermission's cache+invalidate pattern. `GET /seo-settings` is deliberately matrix-free (editor needs the defaults for its preview) with the site key stripped. UI: 4 seed menu rows under Setup; Redirects = plain entity config; SeoSettings + SeoNotFoundLog = custom pages; **SEO Pages keeps the generated list and replaces only its form routes** with SeoPageEditor — new frontend branch 3, no CRUD component forked. Editor has live Google SERP + share-card previews, pixel-width meters, plain-English health checklist, JSON-LD presets in a validating textarea (no new dependency). Verified: `npm test` green (seoResolve.test.js added, ~90 assertions), build green, **66/66 HTTP acceptance + 14/14 permission-matrix checks** on a throwaway local mongo db (dropped after). Caught and fixed 3 Tailwind classes that don't exist in this theme (would have built fine and rendered wrong) and hardened the image-upload URL against the unset `REACT_APP_API_URL` the email-signature upload still trips over. **Not done: browser click-through / dark-mode pass.** Frontend contract written up in `docs/seo-frontend-integration.md`. Also fixed while backfilling: `api-endpoint` skill claimed the permission matrix is not enforced server-side — false since ADR-002, and it sits next to the "guard every route" step. | Human: `npm run seed` per database (4 new menu rows), then a visual pass of the four SEO screens in both themes. Answer Q-8 (is the website server-rendered?) — if it is a client-only SPA, share cards will never work. Then module 3: Reports (system-design). |
| 2026-08-17 | system-design for Dynamic dashboards: ADR-003 (closed widget grammar over code-registered sources; DashboardWidget + RoleDashboard collections; widgetQuery engine reusing buildFilterMatch + buildScopeFilter; dashboards.routes.js; ReportBuilder custom page + Dashboard.jsx renderer; Recharts). Q-5/Q-6 opened. Builds on module 1 — implementation branches from feat/role-data-scoping until PR #3 merges. schema-design done on `feat/dynamic-dashboards`: shared widgets.js constants; DashboardWidget (unique title, source/createdAt indexed, filters embedded bounded); RoleDashboard (unique roleId incl. null=admin default, widgets.widgetId indexed for delete guard); widgetSources registry (users + login-attempts — note LoginAttempt is per-user lock state, not an event log); seed row /report-builder under Setup. No backfill needed — both collections new. Tests pass. api-endpoint done: utils/widgetQuery.js (validateWidget + buildWidgetPipeline, 20+ assertions in widgetQuery.test.js, added to npm test); resolveUserScope extracted from checkPermission for the un-matrixed run path; dashboard.controller.js + dashboards.routes.js (sources/search/preview before /:widgetId; run = admin-or-pinned; role-dashboards me/:roleId/upsert); validation chains; swagger schemas; endpoints.jsx + dashboards.api.jsx. Smoke-tested live on throwaway DB: sources 200, invalid widget 400 with grammar detail, create 201, preview 200, pin 200, run 200, my-dashboard 200, delete-pinned 409. Note for UI: run of an empty collection returns rows [] — stat tile must render 0 (handled). new-page done: recharts installed (approved dep); validated viz palette vars in globals.css (light+dark, .dark-mode scoped); components/ui/widgets.jsx (stat/bar/line/pie/table renderers, theme-aware tooltip, pie folds >8 to Other, WidgetCard states); Dashboard.jsx now renders role's pinned widgets with greeting fallback; ReportBuilder.jsx custom page (library + builder form + debounced live preview + filters editor + pin-to-dashboards panel, permission-gated buttons, ConfirmModal + ReferenceErrorModal); route registered. Build green. Verify done: npm test green (widgetQuery 20+ assertions), build green, 11-check HTTP acceptance on throwaway DB (then dropped) — admin sees both departments, scoped USER sees own dept only with correct label lookup, unpinned-widget probe 403, builder endpoints matrix-403 for ungranted role, empty dashboard returns [] for greeting fallback. ALL PASS. Not done: browser click-through of ReportBuilder/Dashboard incl. dark mode (Chrome extension still not connected) — visual pass recommended; behavior fully covered over HTTP. package-lock change now includes recharts (ours, commit it). update-docs done: ADR-003 accepted with As-built (preview write-gated not ADMIN_ONLY, LoginAttempt finding, stale-widget 409, palette notes); conventions updated (10-architecture where-things-live row, 30-api Dashboard widgets section, 40-frontend custom-page examples + viz palette note). Q-5/Q-6 remain open by design. DOMAIN/RULES/CHECKLISTS/PRD still templates — skipped, starter-level work. Post-ship UX polish (user feedback: page confusing): ReportBuilder restructured into explainer header + 3 numbered steps (build with side-by-side sticky live preview, library grid, dashboards panel), chart-type icon picker with plain-language hints, grouped form sections, preview empty-state says exactly what is missing, cancel-edit + editing banner. Logic unchanged, build green, pushed to PR #4. Shipped: PR #4 (stacked, base feat/role-data-scoping). Merge order: #3 then #4 (retarget #4 to main after #3 merges — GitHub does this automatically). | Human: merge #3 then #4, `npm run seed` per database, visual pass of Report Builder + Dashboard in both themes. Then module 3: Reports (system-design). |
| 2026-08-17 | system-design for Role data scoping: ADR-002 (checkPermission middleware + UserRoles.dataScope + buildScopeFilter/scopeable, per-role scope v1); Q-2..Q-4 opened. schema-design done on `feat/role-data-scoping`: shared scopes.js, UserRoles gains `dataScope` (enum, default "all", no backfill needed) and a unique `{roleId: 1}` index, seed collapses duplicate matrix docs before syncIndexes builds it. Tests pass. api-endpoint done: checkPermission middleware (menuUrl+action per route, 60s cache, invalidated by userRoles/menu controllers), utils/scope.js + runListQuery scopeFilter, session roleId/departmentId at login, createUserRoles now upserts, 65 routes wired across 8 routers, users controller scoped ({department: departmentId, owner: _id}), unauthenticated location GETs guarded (own commit at ship). Dropdown GETs and GET /user-roles/:roleId stay matrix-free — MenuContext loads own matrix at login; gating it would lock all non-admins out. Server booted, unauth /countries now 401. new-page done: Data Scope dropdown on UserRoles.jsx, scope-only save works on empty matrix, build green. Verify done: scope.test.js added to npm test (passes); 18-check HTTP acceptance script run against throwaway Atlas DB `demo-panel-verify` (then dropped) — matrix 403s, department/own/all scoping, cache invalidation on matrix save, MenuContext GET open, admin unchanged: ALL PASS. Not done: browser click-through incl. dark mode (Chrome extension not connected) — 2-min manual pass recommended. update-docs done: ADR-002 accepted with As-built, 10-architecture deviations #1/#2 closed (renumbered), 30-api authorisation section rewritten (matrix + scope conventions), 20-schema tenancy note updated, A-3 recorded (dropdown GETs matrix-free). DOMAIN/RULES/CHECKLISTS/PRD untouched — still templates, starter-level work. Shipped: PR #3 to main (feat + separate location-guard fix commit) on `feat/role-data-scoping`. Awaiting human merge. Uncommitted leftover: pre-existing package-lock.json drift (not ours, left alone). | Human: review/merge PR #3, run `npm run seed` per database, 2-min visual pass of Data Scope dropdown. Then module 2: dynamic dashboards (system-design). |
| 2026-08-17 | Explored "fully dynamic dashboards/reports/analytics" for the starter (explore-idea). Decisions: no raw query builder — closed widget grammar instead (client builds widgets in a builder UI from allowlisted collections/metrics/fields, saves to a library, pins onto per-role dashboards with data scope; Power BI-style UX, not its engine). Recharts approved as the one new admin dependency. Sequence: scope-enforcement foundation → dashboards → reports → analytics. Four module rows added. | Run `system-design` for "Role data scoping" (foundation module). |
| 2026-08-06 | Soft delete (ADR-001): global Mongoose plugin hides deleted records from every read, 11 delete controllers set `isDeleted`, unique indexes made partial, seeder backfills. Verified against a local throwaway database — HTTP delete/re-create/409-guard, admin and non-admin. No admin UI change. | Ship it. **`npm run seed` must be run once against each existing database** or uniqueness silently stops being enforced on old rows. |

---

## Blocked

Anything that cannot proceed, and what would unblock it. Usually a question for the client — those
also belong in `OPEN-QUESTIONS.md`; this table is the short list that is actively stopping work.

| Module | Blocked on | Since |
|---|---|---|
| | | |
